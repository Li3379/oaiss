---
phase: 06-cross-cutting-edge-cases
plan: 03
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - scripts/edge-test.sh
autonomous: true
requirements:
  - EDGE-01
  - EDGE-02
  - EDGE-03
  - EDGE-04
  - EDGE-05
  - EDGE-06

must_haves:
  truths:
    - "Cross-role access blocks unauthorized roles (6-role matrix tested)"
    - "State machine violations return proper error codes for all entity types"
    - "Financial integrity holds across trade/auction operations"
    - "Pagination handles all boundary conditions (empty, single, full, overflow, zero size)"
    - "Input validation rejects negative prices, zero quantities, XSS, SQL injection"
    - "i18n locale files exist for zh-CN and en-US"
  artifacts:
    - path: "scripts/edge-test.sh"
      provides: "Edge case and negative test verification"
      contains: "EDGE-01"
  key_links:
    - from: "scripts/edge-test.sh"
      to: "oaiss-chain-backend (API)"
      via: "curl HTTP requests"
      pattern: "curl.*localhost:8080"
    - from: "scripts/edge-test.sh"
      to: "MySQL"
      via: "db_query for financial integrity checks"
      pattern: "db_query"
---

<objective>
Verify all edge cases and negative testing across 6 dimensions.

Purpose: Per D-09~D-14, this plan creates edge-test.sh covering cross-role access control, state machine violations, financial integrity, pagination boundaries, input validation, and basic i18n. Runs AFTER bugfix and AOP tests per D-18.

Output: scripts/edge-test.sh that verifies EDGE-01~06 and exits 0 on success.
</objective>

<execution_context>
@C:/Users/LiShuai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LiShuai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cross-cutting-edge-cases/06-CONTEXT.md
@.planning/phases/06-cross-cutting-edge-cases/06-RESEARCH.md
@.planning/phases/06-cross-cutting-edge-cases/06-01-SUMMARY.md
@.planning/phases/06-cross-cutting-edge-cases/06-02-SUMMARY.md

@scripts/sign-test.sh

<interfaces>
<!-- Role-to-controller mapping for EDGE-01 cross-role tests -->

<!-- Cross-role access matrix from CONTEXT.md:
  ENTERPRISE(1) can access: CarbonController create/submit, DoubleAuctionController orders, TradeController P2P, CreditScoreController view
  REVIEWER(2) can access: CarbonController review/approve, CreditScoreController admin
  THIRD_PARTY(3) can access: ThirdPartyController all
  ADMIN(4) can access: AdminController all, DoubleAuctionController match, BlockchainController status, CreditScoreController admin
  AUTHENTICATOR(5) can access: BlockchainController status

  Response format: HTTP 200 always, code field varies:
  - 2004 = PERMISSION_DENIED (wrong role)
  - 2000 = USER_NOT_LOGIN (no/invalid token)
  - 3003 = INVALID_STATE_TRANSITION (wrong state)
-->

<!-- State machines for EDGE-02:
  CarbonReport: DRAFT(0) -> SUBMITTED(1) -> UNDER_REVIEW(2) -> APPROVED(3)/REJECTED(4)
  DoubleAuctionOrder: PENDING(0) -> PARTIALLY_MATCHED(1)/FULLY_MATCHED(2)/CANCELLED(3)
  CarbonNeutralProject: DRAFT(0) -> PENDING(1) -> APPROVED(2) -> IMPLEMENTING(3) -> TERMINATED(5), REJECTED(6)
  P2P Trade: PENDING(0) -> ACCEPTED(1)/REJECTED(2) -> COMPLETED(3)

  Error code for invalid transitions: BusinessException(3003) -> code 3003 in response
-->

<!-- Paginated endpoints for EDGE-04:
  GET /carbon/reports?pageNum=1&pageSize=10
  GET /trade/list?pageNum=1&pageSize=10
  GET /auction/orders?pageNum=1&pageSize=10
  GET /admin/users?pageNum=1&pageSize=10
-->

<!-- Input validation endpoints for EDGE-05:
  POST /carbon/reports (CarbonReportRequest)
  POST /auction/orders (AuctionOrderRequest)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/edge-test.sh with EDGE-01~06 verification</name>
  <files>scripts/edge-test.sh</files>
  <read_first>
    scripts/sign-test.sh
    scripts/double-auction-test.sh
    scripts/p2p-trade-test.sh
    oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/CarbonController.java
    oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/DoubleAuctionController.java
    oaiss-chain-frontend/src/i18n/
  </read_first>
  <action>
    Create `scripts/edge-test.sh` following the established sign-test.sh pattern. Pure API testing -- no code modifications needed.

    ```bash
    #!/bin/bash
    # 06-03: Edge Cases & Negative Testing
    # Requirements: EDGE-01~06
    set -euo pipefail
    BASE_URL="http://localhost:8080/api/v1"
    PASS=0; FAIL=0; TEST_ID=0
    ```

    Include assert_contains, assert_not_contains, login_user, db_query helpers.

    **Phase 0: Authentication**
    - Login ALL 5 roles + collect tokens and userIds:
      - enterprise001 (ENTERPRISE), enterprise002 (ENTERPRISE)
      - reviewer001 (REVIEWER)
      - thirdparty001 (THIRD_PARTY)
      - admin (ADMIN)
      - authenticator001 (AUTHENTICATOR)

    **EDGE-01: Cross-role access control (per D-09)**
    Test representative unauthorized access pairs. For each pair, assert response contains `code:2004` (PERMISSION_DENIED) or `code:2000` (if token is rejected before reaching @PreAuthorize). Per RESEARCH.md Pitfall 2: check response body code field, NOT HTTP status.

    Minimum 10 test cases:
    1. ENTERPRISE token -> POST `/carbon/reports/review` (reviewer endpoint) -> assert code 2004
    2. ENTERPRISE token -> POST `/auction/match` (admin endpoint) -> assert code 2004
    3. REVIEWER token -> POST `/carbon/reports` (enterprise endpoint) -> assert code 2004
    4. REVIEWER token -> POST `/auction/orders` (enterprise endpoint) -> assert code 2004
    5. ADMIN token -> POST `/carbon/reports` (enterprise endpoint) -> assert code 2004
    6. THIRD_PARTY token -> POST `/carbon/reports` (enterprise endpoint) -> assert code 2004
    7. ENTERPRISE token -> GET `/admin/users` (admin endpoint) -> assert code 2004
    8. REVIEWER token -> GET `/admin/users` (admin endpoint) -> assert code 2004
    9. AUTHENTICATOR token -> POST `/auction/orders` (enterprise endpoint) -> assert code 2004
    10. Unauthenticated (no token) -> GET `/admin/users` -> assert code 2000

    **EDGE-02: State machine violations (per D-10)**
    Test illegal state transitions. Each test creates an entity in a known state, then attempts an illegal action.

    CarbonReport violations (minimum 4 tests):
    1. Create report (DRAFT) -> attempt to approve directly (skip submit+review) -> assert code 3003 or error
    2. Create report (DRAFT) -> attempt to reject directly -> assert error
    3. Submit report (SUBMITTED) -> attempt to approve directly (skip review) -> assert error
    4. Create + submit + review (UNDER_REVIEW) -> approve -> attempt to re-submit APPROVED report -> assert error

    DoubleAuctionOrder violations (minimum 2 tests):
    1. Create buy order (PENDING) -> attempt to match with no sell orders -> depends on matching logic, at minimum verify status stays PENDING
    2. Create order -> manually cancel -> attempt to match cancelled order -> assert error

    P2P Trade violations (minimum 2 tests):
    1. Enterprise001 creates P2P trade -> Enterprise001 (same user) attempts to accept own trade -> assert error
    2. Create P2P trade -> accept -> attempt to accept again -> assert error

    **EDGE-03: Financial integrity (per D-11)**
    Using db_query to check carbon_coin_account balances before and after operations:

    1. P2P trade: Record enterprise001 and enterprise002 carbon_coin balances before trade. Enterprise001 creates P2P trade offering X coins at Y price. Enterprise002 accepts. Verify:
       - Enterprise001 balance decreased by exactly X
       - Enterprise002 balance increased by exactly X (or by X*Y depending on settlement logic -- check actual TradeService implementation)
       - Sum of both balances unchanged (conservation)

    2. Carbon coin transfer: Record balances. Transfer Z coins from enterprise001 to enterprise002. Verify:
       - Enterprise001 balance decreased by exactly Z
       - Enterprise002 balance increased by exactly Z
       - No fractional loss

    Note: Double-auction financial integrity was tested in Phase 3 (03-02). Focus on P2P and transfer here.

    **EDGE-04: Pagination boundaries (per D-12)**
    Test each paginated endpoint with boundary values. All endpoints use pageNum/pageSize params and return items/total/totalPages.

    For each of these endpoints (carbon reports, trades, admin users):
    1. `pageNum=1&pageSize=1` -- assert returns exactly 1 item (if data exists)
    2. `pageNum=999&pageSize=10` -- assert returns empty items array (page beyond total)
    3. `pageSize=0` -- assert returns error or empty items (behavior depends on Spring Data -- if pageSize=0 is rejected, assert error; if accepted, assert items is empty)
    4. `pageSize=1000` -- assert returns all available items (up to 1000)

    **EDGE-05: Input validation (per D-13)**
    Test endpoints with malicious/invalid inputs:

    1. POST `/auction/orders` (buy) with `unitPrice: -1` -> assert error (negative price)
    2. POST `/auction/orders` (buy) with `quantity: 0` -> assert error (zero quantity)
    3. POST `/auction/orders` (buy) with `quantity: -5` -> assert error (negative quantity)
    4. POST `/auction/orders` (buy) with `unitPrice: 99999999999999999` -> assert error or overflow protection
    5. POST `/carbon/reports` with reportName containing `<script>alert(1)</script>` -> assert either rejected or sanitized (not stored as-is)
    6. POST `/carbon/reports` with reportName containing `' OR 1=1 --` -> assert no SQL error
    7. POST `/carbon/reports` with reportName = 10000 character string -> assert rejected or handled

    **EDGE-06: i18n basic verification (per D-14)**
    Basic verification only (M4 deferred item per CONTEXT.md deferred section):
    1. Check frontend locale files exist: `test -f oaiss-chain-frontend/src/i18n/zh-CN.ts` and `test -f oaiss-chain-frontend/src/i18n/en-US.ts`
    2. Verify each locale file has at least 50 translation keys (basic coverage)
    3. Trigger a backend error (e.g., login with wrong password) and verify the error message is present (may be Chinese or English)

    **Summary section:**
    Print PASS/FAIL count and exit 1 if FAIL > 0.
  </action>
  <acceptance_criteria>
    - `test -f scripts/edge-test.sh` is true
    - `head -1 scripts/edge-test.sh` contains `#!/bin/bash`
    - `grep -c "EDGE-01" scripts/edge-test.sh` is >= 1
    - `grep -c "EDGE-02" scripts/edge-test.sh` is >= 1
    - `grep -c "EDGE-03" scripts/edge-test.sh` is >= 1
    - `grep -c "EDGE-04" scripts/edge-test.sh` is >= 1
    - `grep -c "EDGE-05" scripts/edge-test.sh` is >= 1
    - `grep -c "EDGE-06" scripts/edge-test.sh` is >= 1
    - `grep "assert_contains\|assert_not_contains" scripts/edge-test.sh` exits 0
    - `grep "code.*2004" scripts/edge-test.sh` exits 0 (cross-role access check)
    - `bash scripts/edge-test.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/LiShuai/Desktop/OAISS CHAIN" && bash scripts/edge-test.sh</automated>
  </verify>
  <done>
    scripts/edge-test.sh created and passes all tests. All 6 EDGE requirements verified: cross-role access blocked, state machine violations rejected, financial integrity confirmed, pagination boundaries handled, input validation working, i18n locale files present.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unauthenticated/Unauthorized client -> Protected endpoints | EDGE-01: role-based access enforcement |
| Client input -> Backend validation | EDGE-05: input validation rejects malicious data |
| State machine transitions | EDGE-02: invalid transitions blocked at service layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Spoofing | Role-based access control | mitigate | EDGE-01 tests verify @PreAuthorize blocks unauthorized roles |
| T-06-07 | Tampering | Input validation bypass | mitigate | EDGE-05 tests verify negative/XSS/SQL injection rejected |
| T-06-08 | Tampering | State machine bypass | mitigate | EDGE-02 tests verify illegal transitions rejected with code 3003 |
| T-06-09 | Tampering | Financial manipulation | mitigate | EDGE-03 tests verify balance conservation via DB queries |
</threat_model>

<verification>
1. Script exists and runs: `bash scripts/edge-test.sh`
2. All 6 EDGE sections present in test output
3. 0 failures in final summary
</verification>

<success_criteria>
- scripts/edge-test.sh exits 0 with 0 failures
- At least 10 cross-role access tests pass (EDGE-01)
- At least 8 state machine violation tests pass (EDGE-02)
- Financial integrity confirmed via balance checks (EDGE-03)
- Pagination handles all 4 boundary conditions (EDGE-04)
- Input validation rejects at least 5 malicious inputs (EDGE-05)
- i18n locale files exist for both languages (EDGE-06)
</success_criteria>

<output>
After completion, create `.planning/phases/06-cross-cutting-edge-cases/06-03-SUMMARY.md`
</output>
