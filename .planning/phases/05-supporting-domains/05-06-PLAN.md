# Plan 05-06: Third-Party Monitoring Test Script

**Phase:** 5 — Supporting Domains
**Script:** `scripts/thirdparty-test.sh`
**Requirements:** TP-01, TP-02
**Role:** THIRD_PARTY (thirdparty001)
**Status:** Pending

## Goal

Verify third-party monitoring endpoints: org info, carbon reports, statistics, and contact update via `ThirdPartyController`. TP-02 (trade audit) is partially covered — no dedicated endpoint exists.

## Success Criteria

1. `GET /third-party/org-info` returns 200 with organization data (TP-01)
2. `GET /third-party/statistics` returns 200 with monitoring statistics (TP-01)
3. `GET /third-party/carbon-reports` returns 200 with report list (TP-01, partial TP-02)
4. `GET /third-party/carbon-reports?status=2` filters by status
5. `PUT /third-party/contact` updates contact info — returns 200
6. TP-02 documented as partial coverage in test output

## Approach

Single bash script:
- Login as thirdparty001 (THIRD_PARTY role required — class-level @PreAuthorize)
- Test all 4 endpoints
- Document TP-02 gap in output

## Implementation Steps

1. Create `scripts/thirdparty-test.sh` with boilerplate
2. Login as thirdparty001, extract token
3. **TP-01:** `GET /third-party/org-info` — assert 200, assert org data fields
4. `GET /third-party/statistics` — assert 200, assert statistics fields
5. `GET /third-party/carbon-reports?page=1&size=10` — assert 200, assert report list
6. `GET /third-party/carbon-reports?status=2&page=1&size=10` — assert 200 (filtered)
7. `PUT /third-party/contact` with contactPerson + contactPhone — assert 200
8. Print gap notice: "TP-02 (trade audit): PARTIAL COVERAGE — /carbon-reports as proxy, no dedicated endpoint"
9. Summary with pass/fail counts

## Key Endpoints

| Endpoint | Method | Role | Purpose |
|----------|--------|------|---------|
| `/third-party/org-info` | GET | THIRD_PARTY | Organization info |
| `/third-party/carbon-reports` | GET | THIRD_PARTY | Carbon reports (with filters) |
| `/third-party/statistics` | GET | THIRD_PARTY | Monitoring statistics |
| `/third-party/contact` | PUT | THIRD_PARTY | Update contact info |

## Known Coverage Gaps (D-12)

| Gap | Description | Action |
|-----|-------------|--------|
| TP-02 | Trade audit records — no dedicated endpoint | Mark as partial; /carbon-reports is closest proxy |

## Risk Mitigation

- **Class-level @PreAuthorize:** All endpoints require THIRD_PARTY — must use thirdparty001
- **No trade audit endpoint:** Document clearly, do NOT attempt to create one
- **Contact update:** May modify thirdparty001's contact info — acceptable for test

## Verification

Run `bash scripts/thirdparty-test.sh` — all tests should pass with 0 failures. Coverage gaps printed in output.
