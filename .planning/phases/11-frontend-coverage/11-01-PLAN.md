---
phase: 11-frontend-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - oaiss-chain-frontend/src/api/enterprise.ts
  - oaiss-chain-frontend/src/api/reviewer.ts
  - oaiss-chain-frontend/src/api/auth.ts
  - oaiss-chain-frontend/src/api/user.ts
  - oaiss-chain-frontend/src/api/carbon.ts
  - oaiss-chain-frontend/src/api/trade.ts
  - oaiss-chain-frontend/src/api/auction.ts
  - oaiss-chain-frontend/src/api/carbonCoin.ts
  - oaiss-chain-frontend/src/api/credit.ts
  - oaiss-chain-frontend/src/api/carbonNeutral.ts
  - oaiss-chain-frontend/src/api/blockchain.ts
  - oaiss-chain-frontend/src/api/emission.ts
  - oaiss-chain-frontend/src/api/admin.ts
  - oaiss-chain-frontend/src/api/thirdParty.ts
  - oaiss-chain-frontend/src/api/captcha.ts
  - oaiss-chain-frontend/src/api/signature.ts
autonomous: true
requirements:
  - REQ-09

must_haves:
  truths:
    - "Every backend controller endpoint has a corresponding frontend API function"
    - "All API functions use correct HTTP method, path, and parameter shape matching backend"
    - "Signature encrypt/decrypt parameter shapes match backend controller signatures"
  artifacts:
    - path: "oaiss-chain-frontend/src/api/enterprise.ts"
      provides: "4 EnterpriseController endpoint functions"
      exports: ["getEnterpriseInfo", "getQuotaInfo", "updateContact", "getEnterpriseById"]
    - path: "oaiss-chain-frontend/src/api/reviewer.ts"
      provides: "4 ReviewerController endpoint functions"
      exports: ["getReviewerInfo", "getPendingReports", "getReviewHistory", "getStatistics"]
    - path: "oaiss-chain-frontend/src/api/signature.ts"
      provides: "Fixed encrypt/decrypt parameter shapes"
      contains: "reviewerId"
  key_links:
    - from: "api/enterprise.ts"
      to: "/enterprise/*"
      via: "request.get/post/put"
      pattern: "request\\.(get|post|put).*'/enterprise/"
    - from: "api/reviewer.ts"
      to: "/reviewer/*"
      via: "request.get"
      pattern: "request\\.get.*'/reviewer/"
---

<objective>
Frontend API module coverage -- add all 41 missing endpoint functions and fix 2 parameter mismatches across 16 API files.

Purpose: Plans 11-02 and 11-03 depend on these API functions to wire Enterprise and Reviewer views. Without this plan, views have nothing to call.
Output: 2 new API modules (enterprise.ts, reviewer.ts), 14 updated API modules, 2 parameter shape fixes in signature.ts.
</objective>

<execution_context>
@C:/Users/LiShuai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LiShuai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-frontend-coverage/11-RESEARCH.md

Key pattern from existing API modules (e.g., `carbon.ts`):
```typescript
import request from './request'
import type { RequestType, ResponseType } from '../types'

export function functionName(data: RequestType): Promise<ResponseType> {
  if (!data?.requiredField) return Promise.reject(new Error('Validation message'))
  return request.post('/path', data)       // POST with body
  return request.get('/path', { params })   // GET with query params
  return request.put('/path', data)         // PUT with body
  return request.delete('/path', { params }) // DELETE with query params
}
```

Convention: All functions return `Promise<T>`. The `request.ts` interceptor auto-unwraps `ApiResponse.data` and converts `pageNum/pageSize` to `page/size`. Views access `response.items` and `response.total` (not `response.content`).

Backend encrypt signature: `encryptForReviewer(@RequestBody String data, @RequestParam Long reviewerId)` -- raw string body + query param.
Backend decrypt signature: `decryptData(@RequestBody String encryptedData, @AuthenticationPrincipal ...)` -- raw string body, no extra params.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create enterprise.ts and reviewer.ts, add missing functions to auth/user/carbon/trade/auction/carbonCoin</name>
  <files>
    oaiss-chain-frontend/src/api/enterprise.ts (new)
    oaiss-chain-frontend/src/api/reviewer.ts (new)
    oaiss-chain-frontend/src/api/auth.ts
    oaiss-chain-frontend/src/api/user.ts
    oaiss-chain-frontend/src/api/carbon.ts
    oaiss-chain-frontend/src/api/trade.ts
    oaiss-chain-frontend/src/api/auction.ts
    oaiss-chain-frontend/src/api/carbonCoin.ts
  </files>
  <read_first>
    oaiss-chain-frontend/src/api/carbon.ts
    oaiss-chain-frontend/src/api/auth.ts
    oaiss-chain-frontend/src/api/user.ts
    oaiss-chain-frontend/src/api/trade.ts
    oaiss-chain-frontend/src/api/auction.ts
    oaiss-chain-frontend/src/api/carbonCoin.ts
    oaiss-chain-frontend/src/api/admin.ts
    oaiss-chain-frontend/src/types/auth.ts
    oaiss-chain-frontend/src/types/user.ts
    oaiss-chain-frontend/src/types/carbon.ts
    oaiss-chain-frontend/src/types/trade.ts
  </read_first>
  <action>
**1. Create `src/api/enterprise.ts` (new file)** -- EnterpriseController has 4 endpoints not covered:
```typescript
import request from './request'

export function getEnterpriseInfo(): Promise<unknown> {
  return request.get('/enterprise/info')
}

export function getQuotaInfo(): Promise<unknown> {
  return request.get('/enterprise/quota')
}

export function updateContact(data: { contactPerson: string; contactPhone: string }): Promise<void> {
  if (!data?.contactPerson) return Promise.reject(new Error('联系人不能为空'))
  if (!data?.contactPhone) return Promise.reject(new Error('联系电话不能为空'))
  return request.put('/enterprise/contact', data)
}

export function getEnterpriseById(enterpriseId: number): Promise<unknown> {
  if (!enterpriseId) return Promise.reject(new Error('企业ID不能为空'))
  return request.get(`/enterprise/${enterpriseId}`)
}
```

**2. Create `src/api/reviewer.ts` (new file)** -- ReviewerController has 4 endpoints not covered:
```typescript
import request from './request'
import type { PageRequest } from '../types'

export function getReviewerInfo(): Promise<unknown> {
  return request.get('/reviewer/info')
}

export function getPendingReports(params?: PageRequest): Promise<unknown> {
  return request.get('/reviewer/reports/pending', { params })
}

export function getReviewHistory(params?: PageRequest): Promise<unknown> {
  return request.get('/reviewer/history', { params })
}

export function getStatistics(): Promise<unknown> {
  return request.get('/reviewer/statistics')
}
```

**3. Add to `auth.ts`** -- 3 missing functions after the existing `logout()`:
```typescript
export function register(data: { username: string; password: string; email: string; role: string }): Promise<unknown> {
  if (!data?.username) return Promise.reject(new Error('用户名不能为空'))
  if (!data?.password) return Promise.reject(new Error('密码不能为空'))
  return request.post('/auth/register', data)
}

export function checkIp(): Promise<unknown> {
  return request.get('/auth/check-ip')
}

export function getCurrentUser(): Promise<unknown> {
  return request.get('/auth/me')
}
```

**4. Add to `user.ts`** -- 3 missing functions after existing exports:
```typescript
export function getUserById(userId: number): Promise<unknown> {
  if (!userId) return Promise.reject(new Error('用户ID不能为空'))
  return request.get(`/user/${userId}`)
}

export function checkUsername(username: string): Promise<unknown> {
  if (!username) return Promise.reject(new Error('用户名不能为空'))
  return request.get('/user/check-username', { params: { username } })
}

export function checkEmail(email: string): Promise<unknown> {
  if (!email) return Promise.reject(new Error('邮箱不能为空'))
  return request.get('/user/check-email', { params: { email } })
}
```

**5. Add to `carbon.ts`** -- 1 missing function:
```typescript
export function getReport(reportId: number): Promise<CarbonReportResponse> {
  if (!reportId) return Promise.reject(new Error('报告ID不能为空'))
  return request.get(`/carbon/reports/${reportId}`)
}
```

**6. Add to `trade.ts`** -- 4 missing functions:
```typescript
export function createAuctionTrade(data: TradeRequest): Promise<TradeResponse> {
  if (!data?.quantity || data.quantity <= 0) return Promise.reject(new Error('碳配额数量必须大于0'))
  if (!data?.unitPrice || data.unitPrice <= 0) return Promise.reject(new Error('价格必须大于0'))
  return request.post('/trade/auction', data)
}

export function confirmTrade(tradeId: number): Promise<void> {
  if (!tradeId) return Promise.reject(new Error('交易ID不能为空'))
  return request.post(`/trade/${tradeId}/confirm`)
}

export function getTrade(tradeId: number): Promise<TradeResponse> {
  if (!tradeId) return Promise.reject(new Error('交易ID不能为空'))
  return request.get(`/trade/${tradeId}`)
}

export function listTrades(params?: PageRequest): Promise<unknown> {
  return request.get('/trade/list', { params })
}
```

**7. Add to `auction.ts`** -- 1 missing function:
```typescript
export function executeMatching(): Promise<unknown> {
  return request.post('/auction/match')
}
```

**8. Add to `carbonCoin.ts`** -- 1 missing function:
```typescript
export function recharge(data: { enterpriseId: number; amount: number }): Promise<unknown> {
  if (!data?.enterpriseId) return Promise.reject(new Error('企业ID不能为空'))
  if (!data?.amount || data.amount <= 0) return Promise.reject(new Error('充值金额必须大于0'))
  return request.post('/carbon-coin/recharge', data)
}
```

For all additions: keep existing imports and functions untouched. Append new functions at the end of each file.
  </action>
  <verify>
    <automated>cd "C:/Users/LiShuai/Desktop/OAISS CHAIN/oaiss-chain-frontend" && npx vue-tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - enterprise.ts exists and exports exactly 4 functions: getEnterpriseInfo, getQuotaInfo, updateContact, getEnterpriseById
    - reviewer.ts exists and exports exactly 4 functions: getReviewerInfo, getPendingReports, getReviewHistory, getStatistics
    - auth.ts exports register, checkIp, getCurrentUser (in addition to existing login, logout)
    - user.ts exports getUserById, checkUsername, checkEmail (in addition to existing getProfile, updateProfile, changePassword)
    - carbon.ts exports getReport (in addition to existing 6 functions)
    - trade.ts exports createAuctionTrade, confirmTrade, getTrade, listTrades (in addition to existing 3)
    - auction.ts exports executeMatching (in addition to existing 5)
    - carbonCoin.ts exports recharge (in addition to existing 3)
    - All functions use correct HTTP method and path matching the backend controller endpoints
    - TypeScript compilation passes with zero errors
  </acceptance_criteria>
  <done>All 8 files updated/created with 20 new API functions covering Auth(3), User(3), Carbon(1), Trade(4), Auction(1), CarbonCoin(1), Enterprise(4), Reviewer(4) endpoints. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add missing functions to credit/carbonNeutral/blockchain/emission/admin/thirdParty/captcha, fix signature encrypt/decrypt</name>
  <files>
    oaiss-chain-frontend/src/api/credit.ts
    oaiss-chain-frontend/src/api/carbonNeutral.ts
    oaiss-chain-frontend/src/api/blockchain.ts
    oaiss-chain-frontend/src/api/emission.ts
    oaiss-chain-frontend/src/api/admin.ts
    oaiss-chain-frontend/src/api/thirdParty.ts
    oaiss-chain-frontend/src/api/captcha.ts
    oaiss-chain-frontend/src/api/signature.ts
    oaiss-chain-frontend/src/types/credit.ts
  </files>
  <read_first>
    oaiss-chain-frontend/src/api/credit.ts
    oaiss-chain-frontend/src/api/carbonNeutral.ts
    oaiss-chain-frontend/src/api/blockchain.ts
    oaiss-chain-frontend/src/api/emission.ts
    oaiss-chain-frontend/src/api/admin.ts
    oaiss-chain-frontend/src/api/thirdParty.ts
    oaiss-chain-frontend/src/api/captcha.ts
    oaiss-chain-frontend/src/api/signature.ts
    oaiss-chain-frontend/src/types/credit.ts
    oaiss-chain-frontend/src/types/carbon-neutral.ts
    oaiss-chain-frontend/src/types/carbon-coin.ts
    oaiss-chain-frontend/src/types/emission.ts
    oaiss-chain-frontend/src/types/auth.ts
    oaiss-chain-frontend/src/types/third-party.ts
    oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/DigitalSignatureController.java (lines 255-305 for encrypt/decrypt signatures)
  </read_first>
  <action>
**1. Add 9 functions to `credit.ts`** -- CreditScoreController has 9 missing endpoints. Read the existing file first, then append after `getScoreHistory`:
```typescript
export function getScoreRanking(params?: PageRequest): Promise<unknown> {
  return request.get('/credit/ranking', { params })
}

export function getEnterpriseScore(enterpriseId: number): Promise<CreditScoreResponse> {
  if (!enterpriseId) return Promise.reject(new Error('企业ID不能为空'))
  return request.get(`/credit/${enterpriseId}`)
}

export function getEnterpriseCreditHistory(enterpriseId: number, params?: PageRequest): Promise<CreditEventResponse[]> {
  if (!enterpriseId) return Promise.reject(new Error('企业ID不能为空'))
  return request.get(`/credit/${enterpriseId}/history`, { params })
}

export function deductPoints(data: CreditDeductionRequest): Promise<void> {
  if (!data?.enterpriseId) return Promise.reject(new Error('企业ID不能为空'))
  if (!data?.points || data.points <= 0) return Promise.reject(new Error('扣分数必须大于0'))
  return request.post('/credit/deduct', data)
}

export function addBonus(data: { enterpriseId: number; points: number; reason: string }): Promise<void> {
  if (!data?.enterpriseId) return Promise.reject(new Error('企业ID不能为空'))
  if (!data?.points || data.points <= 0) return Promise.reject(new Error('奖励分数必须大于0'))
  return request.post('/credit/bonus', data)
}

export function evaluateLevel(enterpriseId: number): Promise<CreditScoreResponse> {
  if (!enterpriseId) return Promise.reject(new Error('企业ID不能为空'))
  return request.post(`/credit/evaluate/${enterpriseId}`)
}

export function getRestrictedEnterprises(params?: PageRequest): Promise<unknown> {
  return request.get('/credit/restricted', { params })
}

export function getFrozenEnterprises(params?: PageRequest): Promise<unknown> {
  return request.get('/credit/frozen', { params })
}

export function checkTradePermission(enterpriseId: number): Promise<{ allowed: boolean }> {
  if (!enterpriseId) return Promise.reject(new Error('企业ID不能为空'))
  return request.get(`/credit/check-permission/${enterpriseId}`)
}
```

**2. Add 6 functions to `carbonNeutral.ts`** -- append after existing `terminateProject`:
```typescript
export function getMyProjects(params?: PageRequest): Promise<unknown> {
  return request.get('/carbon-neutral/my', { params })
}

export function reviewProject(projectId: number, data: { approved: boolean; comment: string }): Promise<void> {
  if (!projectId) return Promise.reject(new Error('项目ID不能为空'))
  return request.post(`/carbon-neutral/${projectId}/review`, data)
}

export function verifyProject(data: { projectId: number; verified: boolean; comment: string }): Promise<void> {
  if (!data?.projectId) return Promise.reject(new Error('项目ID不能为空'))
  return request.post('/carbon-neutral/verify', data)
}

export function useCredits(projectId: number, data: { creditAmount: number }): Promise<void> {
  if (!projectId) return Promise.reject(new Error('项目ID不能为空'))
  return request.post(`/carbon-neutral/${projectId}/use-credits`, data)
}

export function completeCertification(projectId: number): Promise<void> {
  if (!projectId) return Promise.reject(new Error('项目ID不能为空'))
  return request.post(`/carbon-neutral/${projectId}/certify`)
}

export function getPendingVerification(params?: PageRequest): Promise<unknown> {
  return request.get('/carbon-neutral/pending-verification', { params })
}
```

**3. Add 2 functions to `blockchain.ts`**:
```typescript
export function queryBlock(blockNumber: number): Promise<unknown> {
  return request.get(`/blockchain/block/${blockNumber}`)
}

export function queryTransaction(txHash: string): Promise<unknown> {
  if (!txHash) return Promise.reject(new Error('交易哈希不能为空'))
  return request.get(`/blockchain/transaction/${txHash}`)
}
```

**4. Add 1 function to `emission.ts`**:
```typescript
export function createRating(data: EmissionRatingRequest): Promise<EmissionRating> {
  if (!data?.enterpriseId) return Promise.reject(new Error('企业ID不能为空'))
  return request.post('/emission/ratings', data)
}
```

**5. Add 3 functions to `admin.ts`**:
```typescript
export function getDashboard(): Promise<unknown> {
  return request.get('/admin/dashboard')
}

export function getConfig(): Promise<unknown> {
  return request.get('/admin/config')
}

export function getPermissions(): Promise<unknown> {
  return request.get('/admin/permissions')
}
```

**6. Add 2 functions to `thirdParty.ts`**:
```typescript
export function getOrgInfo(): Promise<unknown> {
  return request.get('/third-party/org-info')
}

export function updateContact(data: { contactPerson: string; contactPhone: string }): Promise<void> {
  if (!data?.contactPerson) return Promise.reject(new Error('联系人不能为空'))
  return request.put('/third-party/contact', data)
}
```

**7. Add 3 functions to `captcha.ts`**:
```typescript
export function verifyCaptcha(data: { captchaKey: string; captchaCode: string }): Promise<{ valid: boolean }> {
  if (!data?.captchaKey) return Promise.reject(new Error('验证码key不能为空'))
  if (!data?.captchaCode) return Promise.reject(new Error('验证码不能为空'))
  return request.post('/captcha/verify', data)
}

export function sendSmsCode(data: { phone: string }): Promise<void> {
  if (!data?.phone) return Promise.reject(new Error('手机号不能为空'))
  return request.post('/captcha/sms/send', data)
}

export function sendEmailCode(data: { email: string }): Promise<void> {
  if (!data?.email) return Promise.reject(new Error('邮箱不能为空'))
  return request.post('/captcha/email/send', data)
}
```

**8. Fix `signature.ts` encrypt/decrypt** -- replace the two functions to match backend signatures. Backend `encrypt` expects `@RequestBody String data` + `@RequestParam Long reviewerId`. Backend `decrypt` expects `@RequestBody String encryptedData`. Replace:

Current (WRONG):
```typescript
export function encryptData(data: { data: string; publicKey: string }): Promise<{ encrypted: string }> {
  return request.post('/signature/encrypt', data)
}

export function decryptData(data: { data: string; privateKey: string }): Promise<{ decrypted: string }> {
  return request.post('/signature/decrypt', data)
}
```

With (CORRECT -- matching backend):
```typescript
export function encryptData(data: string, reviewerId: number): Promise<string> {
  if (!data) return Promise.reject(new Error('待加密数据不能为空'))
  if (!reviewerId) return Promise.reject(new Error('审核员ID不能为空'))
  return request.post('/signature/encrypt', data, {
    headers: { 'Content-Type': 'text/plain' },
    params: { reviewerId }
  })
}

export function decryptData(encryptedData: string): Promise<string> {
  if (!encryptedData) return Promise.reject(new Error('加密数据不能为空'))
  return request.post('/signature/decrypt', encryptedData, {
    headers: { 'Content-Type': 'text/plain' }
  })
}
```

Note: The backend encrypt/decrypt endpoints use `@RequestBody String` (raw string, not JSON). The frontend must send raw string body with `Content-Type: text/plain` and `encrypt` additionally takes `reviewerId` as a query parameter.

For all additions: keep existing imports and functions untouched. Append new functions at the end of each file. Do NOT remove any existing functions.
  </action>
  <verify>
    <automated>cd "C:/Users/LiShuai/Desktop/OAISS CHAIN/oaiss-chain-frontend" && npx vue-tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - credit.ts exports 11 total functions (2 existing + 9 new): getMyScore, getScoreHistory, getScoreRanking, getEnterpriseScore, getEnterpriseCreditHistory, deductPoints, addBonus, evaluateLevel, getRestrictedEnterprises, getFrozenEnterprises, checkTradePermission
    - carbonNeutral.ts exports 16 total functions (10 existing + 6 new): getMyProjects, reviewProject, verifyProject, useCredits, completeCertification, getPendingVerification added
    - blockchain.ts exports 5 total (3 existing + 2 new): queryBlock, queryTransaction added
    - emission.ts exports 4 total (3 existing + 1 new): createRating added
    - admin.ts exports 10 total (7 existing + 3 new): getDashboard, getConfig, getPermissions added
    - thirdParty.ts exports 4 total (2 existing + 2 new): getOrgInfo, updateContact added
    - captcha.ts exports 4 total (1 existing + 3 new): verifyCaptcha, sendSmsCode, sendEmailCode added
    - signature.ts encryptData signature is `(data: string, reviewerId: number): Promise<string>` -- NOT `{ data, publicKey }`
    - signature.ts decryptData signature is `(encryptedData: string): Promise<string>` -- NOT `{ data, privateKey }`
    - signature.ts encryptData sends Content-Type: text/plain header
    - TypeScript compilation passes with zero errors
    - Total new functions: 26 (Task 1: 20 + Task 2: 26) but Task 2 adds 26, so grand total = 46 new functions covering all 41 missing endpoints + 2 fixes
  </acceptance_criteria>
  <done>All 8 files updated with 26 new API functions. Signature encrypt/decrypt fixed to match backend raw-string body + query-param shape. TypeScript compiles cleanly. Total: 46 new/fixed API functions across 16 files covering all 41 missing endpoints + 2 parameter fixes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> Backend API | All new API functions cross this boundary; request.ts interceptor handles JWT injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | signature.ts encrypt/decrypt | mitigate | Fix parameter shapes to match backend exactly, preventing 400 errors that leak implementation details |
| T-11-02 | Tampering | all new API functions | accept | JWT Bearer auth via request.ts interceptor + backend @PreAuthorize on all endpoints |
| T-11-03 | Spoofing | auth.ts register() | mitigate | Backend validates input; frontend adds client-side validation guards before sending |
</threat_model>

<verification>
1. TypeScript compilation: `cd oaiss-chain-frontend && npx vue-tsc --noEmit` passes with zero errors
2. Count new exports: `grep -c "export function" src/api/enterprise.ts src/api/reviewer.ts` shows 4 each
3. Signature fix verified: `grep "reviewerId" src/api/signature.ts` returns match
4. No existing functions removed: `grep -c "export function" src/api/credit.ts` returns 11 (was 2)
</verification>

<success_criteria>
- All 41 missing backend endpoints have a corresponding frontend API function
- Signature encrypt/decrypt parameter shapes match backend exactly
- TypeScript compiles with zero errors
- No existing API functions removed or altered (except signature.ts encrypt/decrypt fix)
</success_criteria>

<output>
After completion, create `.planning/phases/11-frontend-coverage/11-01-SUMMARY.md`
</output>
