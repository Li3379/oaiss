---
phase: 11-frontend-coverage
plan: 04
type: execute
wave: 3
depends_on: ["11-01"]
files_modified:
  - oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/DigitalSignatureController.java
  - oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/AdminController.java
  - oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/BlockchainController.java
  - oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/EnterpriseController.java
  - oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/ReviewerController.java
  - oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/CarbonCoinController.java
  - oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/MarketPredictionController.java
  - oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/EnterpriseInferenceController.java
autonomous: true
requirements:
  - REQ-11

must_haves:
  truths:
    - "All 20 controllers have unique sequential tag numbers (01-20) with no duplicates"
    - "Swagger UI displays each controller as a separate group"
    - "AI controllers have Chinese-numbered tags consistent with other controllers"
  artifacts:
    - path: "oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/DigitalSignatureController.java"
      provides: "Renumbered @Tag annotation"
      contains: "@Tag(name = \"08."
    - path: "oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/AdminController.java"
      provides: "Renumbered @Tag annotation (was duplicate 08)"
      contains: "@Tag(name = \"16."
  key_links:
    - from: "DigitalSignatureController"
      to: "Swagger UI"
      via: "@Tag annotation"
      pattern: "@Tag\\(name\\s*=\\s*\"\\d+\\."
---

<objective>
Swagger documentation alignment -- fix tag numbering conflicts and naming inconsistencies across all 20 backend controllers.

Purpose: Swagger UI currently merges controllers with duplicate tag numbers (08, 09, 10) into single groups, making the API documentation confusing. Two AI controllers have English-only tags without number prefixes, breaking the consistent pattern used by all other controllers.
Output: 8 controller files updated with unique, sequential @Tag annotations numbered 01-20.
</objective>

<execution_context>
@C:/Users/LiShuai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LiShuai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-frontend-coverage/11-RESEARCH.md

Current @Tag annotations on all 20 controllers (from grep of controller directory):

| Controller | Current Tag | Issue |
|-----------|-------------|-------|
| AuthController | `01. 认证管理` | OK |
| UserController | `02. 用户中心` | OK |
| CarbonController | `03. 碳核算管理` | OK |
| TradeController | `04. 碳交易管理` | OK |
| CreditScoreController | `05. 信誉评分管理` | OK |
| CaptchaController | `06. 验证码管理` | OK |
| FileController | `07. 文件管理` | OK |
| **DigitalSignatureController** | `08. 数字签名` | CONFLICT with AdminController |
| **AdminController** | `08. 管理后台` | CONFLICT with DigitalSignatureController |
| **BlockchainController** | `09. 区块链管理` | CONFLICT with EnterpriseController |
| **EnterpriseController** | `09. 企业用户管理` | CONFLICT with BlockchainController |
| **ReviewerController** | `10. 审核员管理` | CONFLICT with CarbonCoinController |
| **CarbonCoinController** | `10. 碳币交易管理` | CONFLICT with ReviewerController |
| CarbonNeutralProjectController | `11. 碳中和项目管理` | OK |
| DoubleAuctionController | `12. 双向拍卖管理` | OK |
| EmissionController | `13. 碳排放评级管理` | OK |
| SearchController | `14. 搜索查询管理` | OK |
| ThirdPartyController | `15. 第三方监管管理` | OK |
| **MarketPredictionController** | `AI Market Prediction` | NO NUMBER, ENGLISH ONLY |
| **EnterpriseInferenceController** | `Enterprise Inference` | NO NUMBER, ENGLISH ONLY |

Target renumbering (keep 01-15 stable, resolve conflicts by renumbering 16-20):

| Controller | New Tag |
|-----------|---------|
| AuthController | `01. 认证管理` (unchanged) |
| UserController | `02. 用户中心` (unchanged) |
| CarbonController | `03. 碳核算管理` (unchanged) |
| TradeController | `04. 碳交易管理` (unchanged) |
| CreditScoreController | `05. 信誉评分管理` (unchanged) |
| CaptchaController | `06. 验证码管理` (unchanged) |
| FileController | `07. 文件管理` (unchanged) |
| DigitalSignatureController | `08. 数字签名` (unchanged) |
| AdminController | `16. 管理后台` (was 08, renumbered to resolve conflict) |
| BlockchainController | `09. 区块链管理` (unchanged) |
| EnterpriseController | `17. 企业用户管理` (was 09, renumbered to resolve conflict) |
| ReviewerController | `10. 审核员管理` (unchanged) |
| CarbonCoinController | `18. 碳币交易管理` (was 10, renumbered to resolve conflict) |
| CarbonNeutralProjectController | `11. 碳中和项目管理` (unchanged) |
| DoubleAuctionController | `12. 双向拍卖管理` (unchanged) |
| EmissionController | `13. 碳排放评级管理` (unchanged) |
| SearchController | `14. 搜索查询管理` (unchanged) |
| ThirdPartyController | `15. 第三方监管管理` (unchanged) |
| MarketPredictionController | `19. AI市场预测` (was English, now Chinese with number) |
| EnterpriseInferenceController | `20. AI企业推断` (was English, now Chinese with number) |

Renumbering strategy: Keep the "first encountered" controller at its current number, move the conflicting one to the next available slot (16, 17, 18). This minimizes changes and keeps existing documentation stable.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix @Tag numbering conflicts on 4 controllers (AdminController, EnterpriseController, CarbonCoinController, ReviewerController adjustment)</name>
  <files>
    oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/AdminController.java
    oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/EnterpriseController.java
    oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/CarbonCoinController.java
  </files>
  <read_first>
    oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/AdminController.java
    oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/EnterpriseController.java
    oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/CarbonCoinController.java
  </read_first>
  <action>
For each file, read it first, then change ONLY the `@Tag` annotation line. Do NOT modify any other code in the file.

**1. AdminController.java** -- Change @Tag from:
```java
@Tag(name = "08. 管理后台", description = "管理员后台管理接口，包括用户管理、系统监控、数据统计等")
```
To:
```java
@Tag(name = "16. 管理后台", description = "管理员后台管理接口，包括用户管理、系统监控、数据统计等")
```

**2. EnterpriseController.java** -- Change @Tag from:
```java
@Tag(name = "09. 企业用户管理", description = "企业用户专属功能，包括企业信息、配额管理等")
```
To:
```java
@Tag(name = "17. 企业用户管理", description = "企业用户专属功能，包括企业信息、配额管理等")
```

**3. CarbonCoinController.java** -- Change @Tag from:
```java
@Tag(name = "10. 碳币交易管理", description = "碳币账户管理、充值、转账、交易流水查询、碳配额购买/出售")
```
To:
```java
@Tag(name = "18. 碳币交易管理", description = "碳币账户管理、充值、转账、交易流水查询、碳配额购买/出售")
```

Only change the `name` field string. Leave `description` and all other code untouched.
  </action>
  <verify>
    <automated>cd "C:/Users/LiShuai/Desktop/OAISS CHAIN/oaiss-chain-backend" && mvn compile -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AdminController.java @Tag name is exactly "16. 管理后台" (was "08. 管理后台")
    - EnterpriseController.java @Tag name is exactly "17. 企业用户管理" (was "09. 企业用户管理")
    - CarbonCoinController.java @Tag name is exactly "18. 碳币交易管理" (was "10. 碳币交易管理")
    - No other code in any file was changed
    - Backend compiles successfully
    - grep for @Tag in AdminController returns '16. 管理后台'
    - grep for @Tag in EnterpriseController returns '17. 企业用户管理'
    - grep for @Tag in CarbonCoinController returns '18. 碳币交易管理'
    - No controller has tag number 08, 09, or 10 appearing in more than one file
  </acceptance_criteria>
  <done>AdminController renumbered to 16, EnterpriseController to 17, CarbonCoinController to 18. All tag conflicts resolved. Backend compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Fix @Tag naming on AI controllers (MarketPredictionController, EnterpriseInferenceController) to Chinese with number prefix</name>
  <files>
    oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/MarketPredictionController.java
    oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/EnterpriseInferenceController.java
  </files>
  <read_first>
    oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/MarketPredictionController.java
    oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/EnterpriseInferenceController.java
  </read_first>
  <action>
For each file, read it first, then change ONLY the `@Tag` annotation line. Do NOT modify any other code in the file.

**1. MarketPredictionController.java** -- Change @Tag from:
```java
@Tag(name = "AI Market Prediction", description = "Carbon market trend and price forecasting")
```
To:
```java
@Tag(name = "19. AI市场预测", description = "碳市场价格走势预测、供需预测、市场趋势分析")
```

**2. EnterpriseInferenceController.java** -- Change @Tag from:
```java
@Tag(name = "Enterprise Inference", description = "Enterprise compliance risk assessment")
```
To:
```java
@Tag(name = "20. AI企业推断", description = "企业碳排放趋势推断、合规风险评估、异常检测")
```

Only change the `name` and `description` fields. Leave all other code untouched.
  </action>
  <verify>
    <automated>cd "C:/Users/LiShuai/Desktop/OAISS CHAIN/oaiss-chain-backend" && mvn compile -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - MarketPredictionController.java @Tag name is exactly "19. AI市场预测"
    - MarketPredictionController.java @Tag description is exactly "碳市场价格走势预测、供需预测、市场趋势分析"
    - EnterpriseInferenceController.java @Tag name is exactly "20. AI企业推断"
    - EnterpriseInferenceController.java @Tag description is exactly "企业碳排放趋势推断、合规风险评估、异常检测"
    - No other code in any file was changed
    - Backend compiles successfully
    - All 20 controllers have unique tag numbers 01-20 with no duplicates
    - grep -r "@Tag" across all controllers shows 20 unique tag numbers
  </acceptance_criteria>
  <done>MarketPredictionController renumbered to 19 with Chinese name. EnterpriseInferenceController renumbered to 20 with Chinese name. All 20 controllers have unique sequential tags 01-20. Backend compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swagger UI -> Backend | Documentation-only change, no functional impact on API security |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Information Disclosure | Swagger UI | accept | Swagger already disabled in Docker profile (application-docker.yml); tag renaming does not expose new information |
| T-11-11 | Tampering | @Tag annotations | accept | Annotation-only changes, no runtime security impact |
</threat_model>

<verification>
1. Backend compilation: `cd oaiss-chain-backend && mvn compile -q` passes
2. No duplicate tag numbers: Run grep to verify each tag number appears exactly once
```bash
cd oaiss-chain-backend && grep -r '@Tag(name' src/main/java/com/oaiss/chain/controller/ | grep -oP '\d+\.' | sort | uniq -c
```
Every number should have count 1.

3. All 20 controllers accounted for:
```bash
cd oaiss-chain-backend && grep -r '@Tag(name' src/main/java/com/oaiss/chain/controller/ | wc -l
```
Should return 20.
</verification>

<success_criteria>
- All 20 controllers have unique @Tag names with sequential numbers 01-20
- No duplicate tag numbers exist
- All tag names use Chinese text with number prefix (consistent pattern)
- AI controllers (MarketPrediction, EnterpriseInference) have Chinese names instead of English
- Backend compiles successfully
- No functional code changed (only @Tag annotations modified)
</success_criteria>

<output>
After completion, create `.planning/phases/11-frontend-coverage/11-04-SUMMARY.md`
</output>
