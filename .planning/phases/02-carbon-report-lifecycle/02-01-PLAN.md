---
phase: 02-carbon-report-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - oaiss-chain-frontend/src/api/carbon.ts
  - scripts/carbon-report-test.sh
autonomous: true
requirements:
  - CARB-01
  - CARB-02
  - CARB-03
  - CARB-04

must_haves:
  truths:
    - "Enterprise user can create a carbon report with emission data JSON and receive a DRAFT status report with a generated reportNo"
    - "Enterprise user can upload a file via POST /file/upload and include the returned objectName in the report attachments field"
    - "Enterprise user can view their own reports in a paginated list via GET /carbon/my-reports, filtered to their enterpriseId"
    - "Enterprise user can view a single report's full detail including emissionData, totalEmission, and status"
    - "Enterprise user can submit a DRAFT report; status changes from 0 to 1; totalEmission is calculated from emissionData"
  artifacts:
    - path: "oaiss-chain-frontend/src/api/carbon.ts"
      provides: "Fixed reviewReport() field mapping for D-12"
      contains: "reviewResult: data.approved ? 3 : 4"
    - path: "scripts/carbon-report-test.sh"
      provides: "API test script for CARB-01 through CARB-04"
      min_lines: 80
  key_links:
    - from: "scripts/carbon-report-test.sh"
      to: "POST /carbon/reports"
      via: "curl POST with Bearer token"
      pattern: "POST.*carbon/reports"
    - from: "scripts/carbon-report-test.sh"
      to: "POST /file/upload"
      via: "curl multipart upload"
      pattern: "POST.*file/upload"
---

<objective>
Fix the frontend field mapping bug in carbon.ts and create the API test script for enterprise report CRUD (create, list, detail, submit) with real MinIO file upload.

Purpose: The reviewReport() function in carbon.ts has a double bug (validates wrong field name `data.status` instead of mapping `approved` to `reviewResult`). Fixing it now ensures Plan 02-02's review flow will work. The test script validates CARB-01 through CARB04 and creates the report data that Plan 02-02's review tests will consume.

Output: Fixed carbon.ts reviewReport() function, comprehensive carbon-report-test.sh script
</objective>

<execution_context>
@C:/Users/LiShuai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LiShuai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-carbon-report-lifecycle/02-CONTEXT.md
@.planning/phases/02-carbon-report-lifecycle/02-RESEARCH.md

@scripts/login-test.sh

<interfaces>
<!-- Key types and contracts the executor needs. -->

From oaiss-chain-backend/src/main/java/com/oaiss/chain/dto/ReviewRequest.java:
```java
public class ReviewRequest {
    @NotNull private Long reportId;
    @NotNull private Integer reviewResult;  // 3=APPROVED, 4=REJECTED
    @NotBlank private String reviewComment;
}
```

From oaiss-chain-backend/src/main/java/com/oaiss/chain/dto/CarbonReportRequest.java:
```java
public class CarbonReportRequest {
    @NotBlank private String accountingPeriod;  // e.g. "2024-Q1"
    @NotBlank private String title;
    @NotNull private Integer reportType;        // 1=quarterly, 2=annual
    @NotBlank private String emissionData;      // JSON string with scope1/2/3
    private String calculationMethod;
    private String attachments;                 // JSON array string
    private String signatureData;
}
```

From oaiss-chain-frontend/src/api/carbon.ts (CURRENT BROKEN STATE):
```typescript
export function reviewReport(data: ReviewRequest): Promise<void> {
  if (!data?.reportId) return Promise.reject(new Error('报告ID不能为空'))
  if (!data?.status) return Promise.reject(new Error('审核状态不能为空'))  // BUG
  return request.post('/carbon/review', data)  // BUG: sends raw {reviewResult, reviewComment}
}
```

From oaiss-chain-frontend/src/views/auditor/AuditList.vue (CALLER):
```typescript
await reviewReport({
  reportId: reviewForm.value.reportId,
  approved: reviewForm.value.approved,   // boolean
  comment: reviewForm.value.comment,     // string
})
```

API Response Envelope:
```json
{ "code": 200, "message": "...", "data": { ... } }
```

Pagination: frontend sends pageNum/pageSize, backend returns Spring Data Page.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix carbon.ts reviewReport() field mapping bug (D-12)</name>
  <files>oaiss-chain-frontend/src/api/carbon.ts</files>
  <read_first>
    - oaiss-chain-frontend/src/api/carbon.ts
    - oaiss-chain-backend/src/main/java/com/oaiss/chain/dto/ReviewRequest.java
  </read_first>
  <acceptance_criteria>
    - carbon.ts reviewReport function accepts parameter shape `{ reportId: number; approved: boolean; comment: string }`
    - Function body maps `data.approved ? 3 : 4` to `reviewResult` field
    - Function body maps `data.comment` to `reviewComment` field
    - No reference to `data.status` anywhere in the function
    - Function validates `data.reportId` is truthy before sending
  </acceptance_criteria>
  <action>Rewrite the `reviewReport` function in carbon.ts. The current function accepts `ReviewRequest` (which has `reviewResult` and `reviewComment`), but AuditList.vue calls it with `{ reportId, approved (boolean), comment (string) }`. Replace the function with one that accepts `{ reportId: number; approved: boolean; comment: string }`, validates reportId is truthy, and maps the fields before posting: `reviewResult: data.approved ? 3 : 4` and `reviewComment: data.comment`. Remove the broken `data.status` check. Per D-12, the mapping is: `approved: true -> reviewResult: 3`, `approved: false -> reviewResult: 4`, `comment -> reviewComment`.</action>
  <verify>
    <automated>grep -c 'data.status' oaiss-chain-frontend/src/api/carbon.ts</automated>
    Returns 0 (no references to data.status). Then: `grep 'reviewResult: data.approved' oaiss-chain-frontend/src/api/carbon.ts` returns 1 match.
  </verify>
  <done>carbon.ts reviewReport() accepts {reportId, approved, comment}, maps approved->reviewResult (3 or 4) and comment->reviewComment, no data.status reference</done>
</task>

<task type="auto">
  <name>Task 2: Create carbon-report-test.sh script for CARB-01 through CARB-04</name>
  <files>scripts/carbon-report-test.sh</files>
  <read_first>
    - scripts/login-test.sh (reuse ok/fail/info helpers and login pattern)
    - oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/CarbonController.java (endpoint paths, @PreAuthorize roles)
    - oaiss-chain-backend/src/main/java/com/oaiss/chain/controller/FileController.java (upload endpoint)
  </read_first>
  <acceptance_criteria>
    - Script logs in as enterprise001 and enterprise002 (reusing login-test.sh pattern with TOKEN extraction)
    - Script uploads a test file via POST /file/upload (multipart/form-data with -F flag), captures the returned objectName
    - Script creates 3 reports: report1 (enterprise001, will be approved later), report2 (enterprise001, will be rejected later), report3 (enterprise002, stays DRAFT)
    - Each report uses timestamped unique title like "CARB-TEST-{timestamp}" and emissionData JSON with scope1/2/3 arrays
    - Script lists enterprise001's reports via GET /carbon/my-reports and verifies report1 and report2 appear in response
    - Script fetches report1 detail via GET /carbon/reports/{id} and verifies id, title, status=0, emissionData fields are present
    - Script submits report1 and report2 via POST /carbon/reports/{id}/submit, verifies status changes to 1
    - Script prints PASS/FAIL summary at the end
    - Script uses `set -euo pipefail` and the ok/fail/info helper pattern from login-test.sh
    - API base is `http://localhost:8080/api/v1`
  </acceptance_criteria>
  <action>Create `scripts/carbon-report-test.sh` following the login-test.sh pattern (same ok/fail/info helpers, same API base, same TOKEN extraction via grep). The script tests CARB-01 through CARB-04 sequentially:

1. Login as enterprise001 -> TOKEN_E1, enterprise002 -> TOKEN_E2
2. [CARB-01] Upload a test file: `curl -s -X POST "$API/file/upload" -H "Authorization: Bearer $TOKEN_E1" -F "file=@/dev/null;filename=test-evidence.txt;type=text/plain"` -- capture objectName from response. If upload fails (MinIO may not accept empty file), proceed with attachments=null (file upload is best-effort).
3. [CARB-01] Create report1 via `POST /carbon/reports` with TOKEN_E1, body: `{"title":"CARB-TEST-APPROVE-{timestamp}","accountingPeriod":"2024-Q1","reportType":1,"emissionData":"{\"scope1\":[{\"name\":\"gas\",\"activity_data\":1000,\"emission_factor\":2.0}],\"scope2\":[{\"name\":\"electricity\",\"activity_data\":5000,\"emission_factor\":0.6}],\"scope3\":[{\"name\":\"travel\",\"activity_data\":200,\"emission_factor\":0.15}]}","attachments":"[\"$objectName\"]"}`. Extract report1.id from response.
4. [CARB-01] Create report2 via same pattern, title "CARB-TEST-REJECT-{timestamp}", accountingPeriod "2024-Q2".
5. [CARB-01] Create report3 with TOKEN_E2, title "CARB-TEST-ISOLATION-{timestamp}", accountingPeriod "2024-Q3".
6. [CARB-02] GET /carbon/my-reports with TOKEN_E1, verify response contains report1 and report2 IDs, does NOT contain report3 ID.
7. [CARB-03] GET /carbon/reports/{report1.id} with TOKEN_E1, verify response has `"id":report1.id`, `"title":"CARB-TEST-APPROVE-*"`, `"status":0`.
8. [CARB-04] POST /carbon/reports/{report1.id}/submit with TOKEN_E1, verify response status=1 and totalEmission is populated (not null, not 0).
9. [CARB-04] Submit report2 similarly.
10. Print CARB-01 through CARB-04 results.

Use the `grep -o` pattern from login-test.sh for extracting JSON fields. Use `date +%s` for timestamp. Report IDs are extracted from createReport response. Use `mktemp` to create a small temp file for upload if /dev/null upload fails.</action>
  <verify>
    <automated>bash -n scripts/carbon-report-test.sh && echo "syntax ok"</automated>
  </verify>
  <done>carbon-report-test.sh creates 3 reports (2 for enterprise001, 1 for enterprise002), verifies list/detail/submit, prints PASS/FAIL for CARB-01 through CARB-04</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client->API | Untrusted input in report create (title, emissionData, attachments) crosses here |
| API->MinIO | File upload from client through API to object storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | POST /carbon/reports | mitigate | Backend @Valid on CarbonReportRequest validates @NotBlank title, @NotNull reportType, @Size limits |
| T-02-02 | Elevation of privilege | GET /carbon/my-reports | mitigate | Backend filters by enterpriseId from JWT (not request param), enterprise cannot see other enterprise data |
| T-02-03 | Information disclosure | GET /carbon/reports/{id} | accept | Detail endpoint has no @PreAuthorize restriction, any authenticated user can view any report. Acceptable for current phase. |
</threat_model>

<verification>
1. `bash -n scripts/carbon-report-test.sh` -- syntax check passes
2. `grep 'reviewResult: data.approved' oaiss-chain-frontend/src/api/carbon.ts` -- field mapping fix present
3. `grep -c 'data.status' oaiss-chain-frontend/src/api/carbon.ts` -- returns 0
</verification>

<success_criteria>
- carbon.ts reviewReport() maps {approved, comment} to {reviewResult, reviewComment} with no data.status reference
- carbon-report-test.sh syntax-valid, follows login-test.sh pattern, tests CARB-01 through CARB-04
- Script covers: login, file upload, 3 report creates, list verification, detail fetch, submit with status check
</success_criteria>

<output>
After completion, create `.planning/phases/02-carbon-report-lifecycle/02-01-SUMMARY.md`
</output>
