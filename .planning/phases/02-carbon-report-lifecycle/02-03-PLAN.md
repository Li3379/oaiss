---
phase: 02-carbon-report-lifecycle
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - scripts/carbon-report-test.sh
autonomous: false
requirements:
  - CARB-08
  - CARB-09
  - CARB-10
  - CARB-13

must_haves:
  truths:
    - "After approval, the report's blockchainTxHash field contains a non-null value matching pattern tx_mock_*"
    - "After approval, the report's onChainAt field contains a non-null timestamp"
    - "After approval, the report's status is ON_CHAIN(5), not just APPROVED(3)"
    - "After approval, a credit score event was created for the enterprise (verified via API or DB)"
    - "After approval, an emission rating record was created for the enterprise (verified via API or DB)"
    - "Enterprise A's GET /carbon/my-reports does NOT contain Enterprise B's reports"
    - "Full end-to-end flow: create -> submit -> approve -> side effects -> verify -- all 5 success criteria met"
  artifacts:
    - path: "scripts/carbon-report-test.sh"
      provides: "Final comprehensive test with side effect verification and data isolation checks"
      min_lines: 200
  key_links:
    - from: "carbon-report-test.sh verify section"
      to: "CarbonReport.blockchainTxHash"
      via: "API response field check"
      pattern: "blockchainTxHash"
    - from: "carbon-report-test.sh verify section"
      to: "CreditScoreService.addBonusPoints"
      via: "credit score API or DB query after approval"
      pattern: "credit.*score"
---

<objective>
Run the full end-to-end test script and verify cascading side effects actually fire by inspecting API responses and/or database state. This is the final validation that Phase 2's core business flow works.

Purpose: Plan 02-02 wired the code but cannot verify runtime behavior. This plan executes the full script, inspects results, and fixes any issues found. It serves as the phase gate -- all 5 Phase 2 success criteria must pass.

Output: Verified carbon-report-test.sh run with all 13 CARB tests passing, Phase 2 verification report
</objective>

<execution_context>
@C:/Users/LiShuai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LiShuai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-carbon-report-lifecycle/02-CONTEXT.md
@.planning/phases/02-carbon-report-lifecycle/02-RESEARCH.md
@.planning/phases/02-carbon-report-lifecycle/02-01-SUMMARY.md
@.planning/phases/02-carbon-report-lifecycle/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run carbon-report-test.sh, verify side effects, fix failures</name>
  <files>scripts/carbon-report-test.sh</files>
  <read_first>
    - scripts/carbon-report-test.sh (full script as extended in Plans 01 and 02)
    - oaiss-chain-backend/src/main/java/com/oaiss/chain/service/CarbonService.java (verify wiring is correct)
  </read_first>
  <acceptance_criteria>
    - Backend is running and reachable at http://localhost:8080/api/v1/swagger-ui.html
    - Script runs to completion without premature exit (all tests attempted)
    - CARB-01: Report creation returns code 200 with report id and status=0
    - CARB-02: Report list for enterprise contains created report IDs
    - CARB-03: Report detail returns full report with all fields
    - CARB-04: Submit changes status from 0 to 1, totalEmission is calculated
    - CARB-05: Reviewer list with status=1 returns the submitted reports
    - CARB-06: Approval returns status=5 (ON_CHAIN), blockchainTxHash starts with "tx_mock_"
    - CARB-07: Rejection returns status=4 (REJECTED)
    - CARB-08: After approval, credit score side effect verified (CreditEvent created or score change confirmed)
    - CARB-09: After approval, emission rating side effect verified (rating record created)
    - CARB-10: After approval, blockchainTxHash is non-null and matches mock format
    - CARB-11: Illegal state transitions return error responses
    - CARB-12: Authenticator can list but not review
    - CARB-13: Enterprise cannot call review endpoint (403); data isolation holds between enterprises
  </acceptance_criteria>
  <action>Execute the full carbon-report-test.sh and verify all results. Steps:

1. Ensure backend is running: `curl -sf http://localhost:8080/api/v1/swagger-ui.html -o /dev/null`
2. Run the script: `bash scripts/carbon-report-test.sh 2>&1`
3. Examine output. For each CARB test:
   - If PASS: document in verification report
   - If FAIL: diagnose root cause from error output, fix the script or code, re-run
4. Common failure modes and fixes:
   - **Emission rating duplicate error (code 3001):** If a previous test run created an emission rating for the same enterprise+period, the approval will fail. Fix: use unique accounting periods per test run (already handled by timestamped titles, but verify accountingPeriod is unique too).
   - **Credit score cap at 100:** Seed data starts enterprises at score=100. addBonusPoints(+5) is capped at 100 so score won't actually change. This is NOT a bug -- verify the CreditEvent record was created instead.
   - **File upload fails:** MinIO bucket may not exist. The script should handle this gracefully (file upload is best-effort per D-10).
   - **JWT expired mid-test:** Re-login if token expires (45min window from Phase 1). Script re-logins at start, should be fine for a single run.
   - **403 on review:** Verify the enterprise token is not being used for review. Reviewer token must be used.
5. After all tests pass, create the verification report listing each CARB requirement and its result.
6. If the backend compilation is needed after CarbonService changes, run: `cd oaiss-chain-backend && mvn compile -q`
7. If script fixes are needed, edit and re-run until all 13 tests pass.</action>
  <verify>
    <automated>bash scripts/carbon-report-test.sh 2>&1 | tail -20</automated>
    Output shows "Failed: 0" in the summary section.
  </verify>
  <done>All 13 CARB requirements pass in carbon-report-test.sh. Approval path produces ON_CHAIN status with blockchainTxHash. Rejection path produces REJECTED status. Side effects verified. Cross-role access denied. Data isolation confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full carbon report lifecycle test suite (CARB-01 through CARB-13) via API scripts. Backend changes: cascading side effects wired into CarbonService.reviewReport(), frontend field mapping fixed in carbon.ts.</what-built>
  <how-to-verify>
    1. Review the test output: `bash scripts/carbon-report-test.sh` -- all 13 tests should show [OK]
    2. Optionally verify in browser:
       - Login as enterprise001 at http://localhost:5173
       - Navigate to carbon report page
       - Verify the created reports appear in the table with correct status labels
       - Login as reviewer001
       - Navigate to audit list page
       - Verify reports show in the review queue (if any SUBMITTED remain)
       - Try the approve/reject dialog to verify the frontend field mapping fix works through the UI
    3. Check the approval result: the approved report should show "已上链" status and a blockchainTxHash
  </how-to-verify>
  <resume-signal>Type "approved" to confirm Phase 2 is complete, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Full lifecycle | End-to-end data flow from client through API, services, database, and back |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Repudiation | Report review | accept | Reviewer ID and timestamp recorded in report entity. No digital signature on review action in current phase. |
| T-02-08 | Denial of service | Cascading side effects | accept | Sequential synchronous calls could slow response, but acceptable for single-reviewer workflow. |
</threat_model>

<verification>
1. `bash scripts/carbon-report-test.sh 2>&1 | grep -c '\[OK\]'` -- returns at least 10
2. `bash scripts/carbon-report-test.sh 2>&1 | grep '\[FAIL\]'` -- returns empty or 0 count
3. Backend compiles: `cd oaiss-chain-backend && mvn compile -q` exits 0
</verification>

<success_criteria>
- All 13 CARB requirements pass in the test script
- Approval produces ON_CHAIN(5) status with non-null blockchainTxHash matching tx_mock_*
- Rejection produces REJECTED(4) status with reviewComment populated
- Credit score side effect fires (CreditEvent created or score unchanged at cap 100)
- Emission rating side effect fires (rating record created for enterprise+period)
- Enterprise cannot access reviewer endpoints (403)
- Authenticator can list but not review
- Data isolation: enterprise001's list does not contain enterprise002's reports
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/02-carbon-report-lifecycle/02-03-SUMMARY.md`
</output>
