---
phase: 10-admission-qualification
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - oaiss-chain-frontend/src/views/admin/CertificateManage.vue
  - oaiss-chain-frontend/src/api/admin.ts
  - oaiss-chain-frontend/src/router/index.ts
  - oaiss-chain-frontend/src/config/menu.ts
  - oaiss-chain-frontend/src/i18n/locales/zh-CN.ts
  - oaiss-chain-frontend/src/i18n/locales/en-US.ts
  - oaiss-chain-frontend/src/views/enterprise/UserProfile.vue
  - oaiss-chain-frontend/src/views/auditor/AuditList.vue
autonomous: true
requirements:
  - REQ-07
  - REQ-08
must_haves:
  truths:
    - "管理员可在 /admin/certificates 页面查看和管理准入证书与资格证"
    - "管理员可签发企业准入证书（输入企业ID，点击签发）"
    - "管理员可吊销企业准入证书（确认后操作）"
    - "管理员可签发审核员资格证（输入审核员ID，点击签发）"
    - "管理员可吊销审核员资格证（确认后操作）"
    - "企业用户可在个人中心查看自身准入证书状态（已签发/未签发/已吊销）"
    - "审核员可在审核列表页面顶部查看自身资格证状态（已签发/未签发/已吊销）"
  artifacts:
    - path: "oaiss-chain-frontend/src/views/admin/CertificateManage.vue"
      provides: "Admin certificate management page with el-tabs"
      contains: "el-tabs"
    - path: "oaiss-chain-frontend/src/api/admin.ts"
      provides: "API functions for certificate endpoints"
      contains: "getEnterpriseAdmissionList"
    - path: "oaiss-chain-frontend/src/views/enterprise/UserProfile.vue"
      provides: "Enterprise user profile with cert status display"
      contains: "getMyEnterpriseAdmission"
    - path: "oaiss-chain-frontend/src/views/auditor/AuditList.vue"
      provides: "Auditor view with qualification status display"
      contains: "getMyReviewerQualification"
  key_links:
    - from: "CertificateManage.vue"
      to: "api/admin.ts"
      via: "import + function call"
      pattern: "import.*getEnterpriseAdmissionList.*from.*api/admin"
    - from: "api/admin.ts"
      to: "/api/v1/admin/enterprise-admission"
      via: "axios request"
      pattern: "request\\.get\\('/admin/enterprise-admission"
    - from: "UserProfile.vue"
      to: "api/admin.ts"
      via: "import + function call"
      pattern: "import.*getMyEnterpriseAdmission.*from.*api/admin"
    - from: "AuditList.vue"
      to: "api/admin.ts"
      via: "import + function call"
      pattern: "import.*getMyReviewerQualification.*from.*api/admin"
---

<objective>
Create frontend pages for certificate management (admin) and certificate status display (enterprise/auditor).

Purpose: Complete the user-facing layer for Phase 10. Admins manage certificates via a tabbed page. Enterprises and reviewers see their own certificate status.

Output: 1 new Vue page, 8 new API functions, router/menu updates, 2 existing pages modified with cert status display, i18n translations.
</objective>

<execution_context>
@C:/Users/LiShuai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LiShuai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-admission-qualification/10-CONTEXT.md
@.planning/phases/10-admission-qualification/10-PATTERNS.md
@.planning/phases/10-admission-qualification/10-01-SUMMARY.md
@.planning/phases/10-admission-qualification/10-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From oaiss-chain-frontend/src/api/admin.ts (existing):
```typescript
import request from './request'
import type { PageRequest } from '../types'

export function getUserList(params?: PageRequest): Promise<unknown> {
  return request.get('/admin/users', { params })
}
export function updateUserStatus(userId: number, status: number): Promise<void> {
  return request.put(`/admin/users/${userId}/status`, null, { params: { status } })
}
export function getStatistics(): Promise<unknown> {
  return request.get('/admin/statistics')
}
```

From oaiss-chain-frontend/src/views/admin/SystemUsers.vue (analog pattern):
```vue
<script setup lang="ts">
import { computed, onMounted, reactive, ref } from 'vue'
import { useI18n } from 'vue-i18n'
import { ElMessage, ElMessageBox } from 'element-plus'
// fetchData pattern: loading ref, currentPage ref, pageSize ref, total ref
// el-table with v-loading, el-tag for status, el-pagination
// ElMessageBox.confirm() before destructive actions
// ElMessage.success/error for feedback
</script>
<template>
  <section class="xxx-page">
    <el-card class="section-card" shadow="never">
      <el-breadcrumb separator="/">...</el-breadcrumb>
    </el-card>
    <el-card class="section-card" shadow="never">
      <el-table :data="dataList" border v-loading="loading">...</el-table>
      <div class="pagination-row">
        <el-pagination v-model:current-page="currentPage" v-model:page-size="pageSize"
          background :page-sizes="[10, 20, 50]"
          layout="total, sizes, prev, pager, next, jumper" :total="total"
          @size-change="onSizeChange" @current-change="onCurrentChange" />
      </div>
    </el-card>
  </section>
</template>
<style scoped>
.xxx-page { display: flex; flex-direction: column; gap: 14px; }
.section-card { border: 1px solid var(--border-color); border-radius: 12px; }
.pagination-row { margin-top: 14px; display: flex; justify-content: flex-end; }
</style>
```

From oaiss-chain-frontend/src/router/index.ts (admin route pattern):
```typescript
{
  path: 'admin/system/users',
  name: 'AdminSystemUsers',
  component: () => import('../views/admin/SystemUsers.vue'),
  meta: { title: '用户管理', roles: [ROLE.ADMIN] },
},
```

From oaiss-chain-frontend/src/config/menu.ts (ADMIN menu):
```typescript
[ROLE.ADMIN]: [
  {
    label: 'menu.admin',
    children: [
      { label: 'menu.systemManage', children: [
        { label: 'menu.userManage', path: '/admin/system/users' },
        { label: 'menu.carbonManage', path: '/admin/system/carbon' },
        { label: 'menu.systemConfig', path: '/admin/system/config' },
      ] },
      { label: 'menu.dataManage', children: [{ label: 'menu.statisticsData', path: '/admin/data/statistics' }] },
      { label: 'menu.certificationManage', children: [{ label: 'menu.certificationList', path: '/admin/verify/list' }] },
    ],
  },
],
```

From oaiss-chain-frontend/src/config/menu.ts (ROLE constant):
```typescript
export const ROLE = {
  ENTERPRISE: 'ENTERPRISE',
  REVIEWER: 'REVIEWER',
  THIRD_PARTY: 'THIRD_PARTY',
  ADMIN: 'ADMIN',
} as const
```

From oaiss-chain-frontend/src/api/request.ts (interceptor behavior):
- Auto-transforms pageNum/pageSize to page/size
- Auto-unwraps ApiResponse.data
- Transforms Spring Page to { items, total, page, size, totalPages }
- API functions use Promise<unknown> return type
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API functions + i18n translations + router/menu config</name>
  <files>
    oaiss-chain-frontend/src/api/admin.ts,
    oaiss-chain-frontend/src/i18n/locales/zh-CN.ts,
    oaiss-chain-frontend/src/i18n/locales/en-US.ts,
    oaiss-chain-frontend/src/router/index.ts,
    oaiss-chain-frontend/src/config/menu.ts
  </files>
  <read_first>
    oaiss-chain-frontend/src/api/admin.ts,
    oaiss-chain-frontend/src/i18n/locales/zh-CN.ts,
    oaiss-chain-frontend/src/i18n/locales/en-US.ts,
    oaiss-chain-frontend/src/router/index.ts,
    oaiss-chain-frontend/src/config/menu.ts
  </read_first>
  <action>
    **Step 1: Add 8 API functions to `api/admin.ts`** per D-03.

    Append after existing functions:

    ```typescript
    // ============ Enterprise Admission (准入证书) ============

    export function getEnterpriseAdmissionList(params?: PageRequest): Promise<unknown> {
      return request.get('/admin/enterprise-admission', { params })
    }

    export function issueEnterpriseAdmission(enterpriseId: number): Promise<unknown> {
      return request.post(`/admin/enterprise-admission/${enterpriseId}/issue`)
    }

    export function revokeEnterpriseAdmission(enterpriseId: number): Promise<void> {
      return request.delete(`/admin/enterprise-admission/${enterpriseId}`)
    }

    export function getMyEnterpriseAdmission(): Promise<unknown> {
      return request.get('/admin/enterprise-admission/my')
    }

    // ============ Reviewer Qualification (审核员资格证) ============

    export function getReviewerQualificationList(params?: PageRequest): Promise<unknown> {
      return request.get('/admin/reviewer-qualification', { params })
    }

    export function issueReviewerQualification(reviewerId: number): Promise<unknown> {
      return request.post(`/admin/reviewer-qualification/${reviewerId}/issue`)
    }

    export function revokeReviewerQualification(reviewerId: number): Promise<void> {
      return request.delete(`/admin/reviewer-qualification/${reviewerId}`)
    }

    export function getMyReviewerQualification(): Promise<unknown> {
      return request.get('/admin/reviewer-qualification/my')
    }
    ```

    **Step 2: Add i18n translations to `zh-CN.ts`** (append new `certificateManage` section before closing `}`):

    ```typescript
    certificateManage: {
      breadcrumbAdmin: '管理员',
      breadcrumbCertificate: '证书管理',
      tabAdmission: '准入证书',
      tabQualification: '审核员资格证',
      colEnterpriseId: '企业ID',
      colReviewerId: '审核员ID',
      colCertificateNo: '证书编号',
      colIssuedDate: '签发日期',
      colStatus: '状态',
      colCreateTime: '创建时间',
      colOperation: '操作',
      active: '有效',
      revoked: '已吊销',
      notIssued: '未签发',
      issue: '签发',
      revoke: '吊销',
      enterEnterpriseId: '请输入企业ID',
      enterReviewerId: '请输入审核员ID',
      confirmRevoke: '确定要吊销该证书吗？',
      revokeTitle: '吊销确认',
      issueSuccess: '签发成功',
      issueFailed: '签发失败',
      revokeSuccess: '吊销成功',
      revokeFailed: '吊销失败',
      loadFailed: '加载证书列表失败',
      certStatus: '证书状态',
      myAdmission: '我的准入证书',
      myQualification: '我的资格证',
      certNo: '证书编号',
      issuedDate: '签发日期',
    },
    ```

    **Step 3: Add i18n translations to `en-US.ts`** (append new `certificateManage` section):

    ```typescript
    certificateManage: {
      breadcrumbAdmin: 'Admin',
      breadcrumbCertificate: 'Certificate Management',
      tabAdmission: 'Admission',
      tabQualification: 'Reviewer Qualification',
      colEnterpriseId: 'Enterprise ID',
      colReviewerId: 'Reviewer ID',
      colCertificateNo: 'Certificate No',
      colIssuedDate: 'Issued Date',
      colStatus: 'Status',
      colCreateTime: 'Created At',
      colOperation: 'Operation',
      active: 'Active',
      revoked: 'Revoked',
      notIssued: 'Not Issued',
      issue: 'Issue',
      revoke: 'Revoke',
      enterEnterpriseId: 'Enter Enterprise ID',
      enterReviewerId: 'Enter Reviewer ID',
      confirmRevoke: 'Are you sure to revoke this certificate?',
      revokeTitle: 'Revoke Confirmation',
      issueSuccess: 'Issued successfully',
      issueFailed: 'Issuance failed',
      revokeSuccess: 'Revoked successfully',
      revokeFailed: 'Revocation failed',
      loadFailed: 'Failed to load certificate list',
      certStatus: 'Certificate Status',
      myAdmission: 'My Admission',
      myQualification: 'My Qualification',
      certNo: 'Certificate No',
      issuedDate: 'Issued Date',
    },
    ```

    **Step 4: Add route to `router/index.ts`** per D-05.

    Add inside the `children` array of the layout route (after the last admin route, before the closing `]`):

    ```typescript
    {
      path: 'admin/certificates',
      name: 'AdminCertificates',
      component: () => import('../views/admin/CertificateManage.vue'),
      meta: { title: '证书管理', roles: [ROLE.ADMIN] },
    },
    ```

    **Step 5: Add menu item to `config/menu.ts`** per D-05.

    In the ADMIN section, add a new menu group after `certificationManage`:

    ```typescript
    { label: 'menu.certificateManage', children: [{ label: 'menu.certificateList', path: '/admin/certificates' }] },
    ```

    Also add the corresponding i18n keys to both locale files in the `menu` section:
    - zh-CN: `certificateManage: '证书管理'`, `certificateList: '证书列表'`
    - en-US: `certificateManage: 'Certificate Mgmt'`, `certificateList: 'Certificate List'`
  </action>
  <verify>
    <automated>cd oaiss-chain-frontend && npx vue-tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `api/admin.ts` contains `getEnterpriseAdmissionList`
    - File `api/admin.ts` contains `issueEnterpriseAdmission`
    - File `api/admin.ts` contains `revokeEnterpriseAdmission`
    - File `api/admin.ts` contains `getMyEnterpriseAdmission`
    - File `api/admin.ts` contains `getReviewerQualificationList`
    - File `api/admin.ts` contains `issueReviewerQualification`
    - File `api/admin.ts` contains `revokeReviewerQualification`
    - File `api/admin.ts` contains `getMyReviewerQualification`
    - File `router/index.ts` contains `path: 'admin/certificates'`
    - File `router/index.ts` contains `AdminCertificates`
    - File `config/menu.ts` contains `certificateManage`
    - File `zh-CN.ts` contains `certificateManage` section
    - File `en-US.ts` contains `certificateManage` section
    - File `zh-CN.ts` contains `menu.certificateManage` key
    - File `en-US.ts` contains `menu.certificateManage` key
  </acceptance_criteria>
  <done>API functions, i18n translations, route, and menu config all created. TypeScript check passes.</done>
</task>

<task type="auto">
  <name>Task 2: CertificateManage.vue admin page</name>
  <files>
    oaiss-chain-frontend/src/views/admin/CertificateManage.vue
  </files>
  <read_first>
    oaiss-chain-frontend/src/views/admin/SystemUsers.vue,
    oaiss-chain-frontend/src/api/admin.ts
  </read_first>
  <action>
    **Create `views/admin/CertificateManage.vue`** per D-05.

    Follow SystemUsers.vue pattern exactly. Use `<script setup lang="ts">` with `useI18n()`.

    **Script section:**
    - Imports: `getEnterpriseAdmissionList, issueEnterpriseAdmission, revokeEnterpriseAdmission, getReviewerQualificationList, issueReviewerQualification, revokeReviewerQualification` from `../../api/admin`
    - State: `activeTab` ref ('admission'), separate state for each tab: `admissionList`, `qualificationList`, `admissionLoading`, `qualificationLoading`, `admissionPage`, `qualificationPage`, `admissionPageSize`, `qualificationPageSize`, `admissionTotal`, `qualificationTotal`
    - Issue form: `issueDialogVisible` ref, `issueForm` reactive `{ enterpriseId: '', reviewerId: '' }`
    - `fetchAdmissionData()` -- calls `getEnterpriseAdmissionList({ page: admissionPage.value, size: admissionPageSize.value })`, sets admissionList and admissionTotal
    - `fetchQualificationData()` -- calls `getReviewerQualificationList({ page: qualificationPage.value, size: qualificationPageSize.value })`, sets qualificationList and qualificationTotal
    - `onTabChange(tab)` -- triggers fetch for the active tab
    - `handleIssueAdmission()` -- validates enterpriseId, calls `issueEnterpriseAdmission(issueForm.enterpriseId)`, shows success/error message, refreshes list
    - `handleIssueQualification()` -- validates reviewerId, calls `issueReviewerQualification(issueForm.reviewerId)`, shows success/error message, refreshes list
    - `handleRevokeAdmission(row)` -- ElMessageBox.confirm -> `revokeEnterpriseAdmission(row.enterpriseId)` -> refresh
    - `handleRevokeQualification(row)` -- ElMessageBox.confirm -> `revokeReviewerQualification(row.reviewerId)` -> refresh
    - `getStatusType(status)` -- returns 'success' for 1, 'danger' for 2
    - `getStatusText(status)` -- returns t('certificateManage.active') for 1, t('certificateManage.revoked') for 2
    - Pagination handlers for each tab
    - `onMounted(() => fetchAdmissionData())`

    **Template section:**
    ```vue
    <template>
      <section class="certificate-page">
        <el-card class="section-card" shadow="never">
          <el-breadcrumb separator="/">
            <el-breadcrumb-item>{{ t('certificateManage.breadcrumbAdmin') }}</el-breadcrumb-item>
            <el-breadcrumb-item>{{ t('certificateManage.breadcrumbCertificate') }}</el-breadcrumb-item>
          </el-breadcrumb>
        </el-card>

        <el-card class="section-card" shadow="never">
          <el-tabs v-model="activeTab" @tab-change="onTabChange">
            <!-- Admission Tab -->
            <el-tab-pane :label="t('certificateManage.tabAdmission')" name="admission">
              <div style="margin-bottom: 14px;">
                <el-button type="primary" @click="issueDialogVisible = true; issueType = 'admission'">
                  {{ t('certificateManage.issue') }}
                </el-button>
              </div>
              <el-table :data="admissionList" border v-loading="admissionLoading">
                <el-table-column prop="id" label="ID" width="80" />
                <el-table-column prop="enterpriseId" :label="t('certificateManage.colEnterpriseId')" min-width="120" />
                <el-table-column prop="certificateNo" :label="t('certificateManage.colCertificateNo')" min-width="180" />
                <el-table-column prop="issuedDate" :label="t('certificateManage.colIssuedDate')" min-width="120" />
                <el-table-column prop="status" :label="t('certificateManage.colStatus')" min-width="100">
                  <template #default="{ row }">
                    <el-tag :type="getStatusType(row.status)">{{ getStatusText(row.status) }}</el-tag>
                  </template>
                </el-table-column>
                <el-table-column prop="createdAt" :label="t('certificateManage.colCreateTime')" min-width="170" />
                <el-table-column :label="t('certificateManage.colOperation')" width="120" fixed="right">
                  <template #default="{ row }">
                    <el-button v-if="row.status === 1" link type="danger" @click="handleRevokeAdmission(row)">
                      {{ t('certificateManage.revoke') }}
                    </el-button>
                  </template>
                </el-table-column>
              </el-table>
              <div class="pagination-row">
                <el-pagination v-model:current-page="admissionPage" v-model:page-size="admissionPageSize"
                  background :page-sizes="[10, 20, 50]"
                  layout="total, sizes, prev, pager, next, jumper" :total="admissionTotal"
                  @size-change="onAdmissionSizeChange" @current-change="onAdmissionCurrentChange" />
              </div>
            </el-tab-pane>

            <!-- Qualification Tab -->
            <el-tab-pane :label="t('certificateManage.tabQualification')" name="qualification">
              <div style="margin-bottom: 14px;">
                <el-button type="primary" @click="issueDialogVisible = true; issueType = 'qualification'">
                  {{ t('certificateManage.issue') }}
                </el-button>
              </div>
              <el-table :data="qualificationList" border v-loading="qualificationLoading">
                <el-table-column prop="id" label="ID" width="80" />
                <el-table-column prop="reviewerId" :label="t('certificateManage.colReviewerId')" min-width="120" />
                <el-table-column prop="certificateNo" :label="t('certificateManage.colCertificateNo')" min-width="180" />
                <el-table-column prop="qualificationType" label="资质类型" min-width="150" />
                <el-table-column prop="issuedDate" :label="t('certificateManage.colIssuedDate')" min-width="120" />
                <el-table-column prop="status" :label="t('certificateManage.colStatus')" min-width="100">
                  <template #default="{ row }">
                    <el-tag :type="getStatusType(row.status)">{{ getStatusText(row.status) }}</el-tag>
                  </template>
                </el-table-column>
                <el-table-column prop="createdAt" :label="t('certificateManage.colCreateTime')" min-width="170" />
                <el-table-column :label="t('certificateManage.colOperation')" width="120" fixed="right">
                  <template #default="{ row }">
                    <el-button v-if="row.status === 1" link type="danger" @click="handleRevokeQualification(row)">
                      {{ t('certificateManage.revoke') }}
                    </el-button>
                  </template>
                </el-table-column>
              </el-table>
              <div class="pagination-row">
                <el-pagination v-model:current-page="qualificationPage" v-model:page-size="qualificationPageSize"
                  background :page-sizes="[10, 20, 50]"
                  layout="total, sizes, prev, pager, next, jumper" :total="qualificationTotal"
                  @size-change="onQualificationSizeChange" @current-change="onQualificationCurrentChange" />
              </div>
            </el-tab-pane>
          </el-tabs>
        </el-card>

        <!-- Issue Dialog -->
        <el-dialog v-model="issueDialogVisible" :title="t('certificateManage.issue')" width="400px">
          <el-form v-if="issueType === 'admission'">
            <el-form-item :label="t('certificateManage.colEnterpriseId')">
              <el-input v-model="issueForm.enterpriseId" :placeholder="t('certificateManage.enterEnterpriseId')" />
            </el-form-item>
          </el-form>
          <el-form v-else>
            <el-form-item :label="t('certificateManage.colReviewerId')">
              <el-input v-model="issueForm.reviewerId" :placeholder="t('certificateManage.enterReviewerId')" />
            </el-form-item>
          </el-form>
          <template #footer>
            <el-button @click="issueDialogVisible = false">{{ t('common.cancel') }}</el-button>
            <el-button type="primary" @click="issueType === 'admission' ? handleIssueAdmission() : handleIssueQualification()">
              {{ t('common.confirm') }}
            </el-button>
          </template>
        </el-dialog>
      </section>
    </template>
    ```

    **Style section:** Follow SystemUsers.vue pattern with `.certificate-page`, `.section-card`, `.pagination-row`.
  </action>
  <verify>
    <automated>cd oaiss-chain-frontend && npx vue-tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `CertificateManage.vue` exists
    - File contains `el-tabs` with `v-model="activeTab"`
    - File contains `el-tab-pane` for admission tab
    - File contains `el-tab-pane` for qualification tab
    - File contains `el-table` for admission data
    - File contains `el-table` for qualification data
    - File contains `el-pagination` for each tab
    - File contains `el-dialog` for issue form
    - File contains `ElMessageBox.confirm` for revoke confirmation
    - File contains `getStatusType` returning 'success'/'danger'
    - File contains imports from `../../api/admin`
    - File contains `el-tag` for status display
    - File contains `el-breadcrumb` navigation
  </acceptance_criteria>
  <done>CertificateManage.vue created with dual-tab layout, issue/revoke dialogs, pagination, and status tags.</done>
</task>

<task type="auto">
  <name>Task 3: Enterprise and auditor certificate status display</name>
  <files>
    oaiss-chain-frontend/src/views/enterprise/UserProfile.vue,
    oaiss-chain-frontend/src/views/auditor/AuditList.vue
  </files>
  <read_first>
    oaiss-chain-frontend/src/views/enterprise/UserProfile.vue,
    oaiss-chain-frontend/src/views/auditor/AuditList.vue,
    oaiss-chain-frontend/src/api/admin.ts
  </read_first>
  <action>
    **Step 1: Add certificate status to `enterprise/UserProfile.vue`**

    The UserProfile.vue already shows user info. Add a certificate status card after the existing content.

    In `<script setup>`, add:
    ```typescript
    import { getMyEnterpriseAdmission } from '../../api/admin'

    const admissionStatus = ref(null)  // null = not issued, object = issued
    const admissionLoading = ref(false)

    const fetchAdmissionStatus = async () => {
      admissionLoading.value = true
      try {
        const res = await getMyEnterpriseAdmission()
        // res is a list; if empty = not issued, otherwise show latest
        const list = Array.isArray(res) ? res : (res?.items || [])
        admissionStatus.value = list.length > 0 ? list[0] : null
      } catch {
        admissionStatus.value = null
      } finally {
        admissionLoading.value = false
      }
    }

    const admissionStatusType = computed(() => {
      if (!admissionStatus.value) return 'info'
      return admissionStatus.value.status === 1 ? 'success' : 'danger'
    })

    const admissionStatusText = computed(() => {
      if (!admissionStatus.value) return t('certificateManage.notIssued')
      return admissionStatus.value.status === 1 ? t('certificateManage.active') : t('certificateManage.revoked')
    })

    onMounted(() => {
      // ... existing onMounted logic ...
      fetchAdmissionStatus()
    })
    ```

    In `<template>`, add a certificate status section (after existing content, before closing tag):
    ```vue
    <el-card class="section-card" shadow="never" style="margin-top: 14px;">
      <template #header>
        <span>{{ t('certificateManage.myAdmission') }}</span>
      </template>
      <el-descriptions :column="2" border>
        <el-descriptions-item :label="t('certificateManage.certStatus')">
          <el-tag :type="admissionStatusType">{{ admissionStatusText }}</el-tag>
        </el-descriptions-item>
        <el-descriptions-item v-if="admissionStatus.value" :label="t('certificateManage.certNo')">
          {{ admissionStatus.value.certificateNo }}
        </el-descriptions-item>
        <el-descriptions-item v-if="admissionStatus.value" :label="t('certificateManage.issuedDate')">
          {{ admissionStatus.value.issuedDate }}
        </el-descriptions-item>
      </el-descriptions>
    </el-card>
    ```

    **Step 2: Add qualification status to `auditor/AuditList.vue`**

    Add a qualification status banner at the top of the page.

    In `<script setup>`, add:
    ```typescript
    import { getMyReviewerQualification } from '../../api/admin'

    const qualificationStatus = ref(null)
    const qualificationLoading = ref(false)

    const fetchQualificationStatus = async () => {
      qualificationLoading.value = true
      try {
        const res = await getMyReviewerQualification()
        const list = Array.isArray(res) ? res : (res?.items || [])
        qualificationStatus.value = list.length > 0 ? list[0] : null
      } catch {
        qualificationStatus.value = null
      } finally {
        qualificationLoading.value = false
      }
    }

    const qualificationStatusType = computed(() => {
      if (!qualificationStatus.value) return 'info'
      return qualificationStatus.value.status === 1 ? 'success' : 'danger'
    })

    const qualificationStatusText = computed(() => {
      if (!qualificationStatus.value) return t('certificateManage.notIssued')
      return qualificationStatus.value.status === 1 ? t('certificateManage.active') : t('certificateManage.revoked')
    })

    onMounted(() => {
      // ... existing onMounted logic ...
      fetchQualificationStatus()
    })
    ```

    In `<template>`, add a status banner at the top (before existing content):
    ```vue
    <el-card class="section-card" shadow="never" style="margin-bottom: 14px;">
      <el-space>
        <span>{{ t('certificateManage.myQualification') }}:</span>
        <el-tag :type="qualificationStatusType">{{ qualificationStatusText }}</el-tag>
        <span v-if="qualificationStatus.value" style="color: #999; font-size: 13px;">
          {{ qualificationStatus.value.certificateNo }}
        </span>
      </el-space>
    </el-card>
    ```

    Also add `computed` to the vue import if not already present.
  </action>
  <verify>
    <automated>cd oaiss-chain-frontend && npx vue-tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `UserProfile.vue` contains `getMyEnterpriseAdmission` import
    - File `UserProfile.vue` contains `admissionStatus` ref
    - File `UserProfile.vue` contains `el-tag` with `admissionStatusType`
    - File `UserProfile.vue` contains `certificateManage.myAdmission` i18n key usage
    - File `UserProfile.vue` contains `el-descriptions` for cert details
    - File `AuditList.vue` contains `getMyReviewerQualification` import
    - File `AuditList.vue` contains `qualificationStatus` ref
    - File `AuditList.vue` contains `el-tag` with `qualificationStatusType`
    - File `AuditList.vue` contains `certificateManage.myQualification` i18n key usage
    - Both files compile without TypeScript errors
  </acceptance_criteria>
  <done>Enterprise UserProfile shows admission cert status. Auditor AuditList shows qualification status. Both display el-tag with color-coded status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin->frontend | Admin manages certificates through authenticated frontend |
| ENTERPRISE->frontend | Enterprise views own cert status via /my API |
| REVIEWER->frontend | Reviewer views own qualification status via /my API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-14 | Information Disclosure | CertificateManage.vue | mitigate | Page route restricted to ROLE.ADMIN via router meta.roles. Backend @PreAuthorize enforces access. |
| T-10-15 | Elevation of Privilege | UserProfile.vue cert display | mitigate | getMyEnterpriseAdmission() calls backend /my endpoint which requires ENTERPRISE role JWT. Frontend only displays what backend returns. |
| T-10-16 | Elevation of Privilege | AuditList.vue cert display | mitigate | getMyReviewerQualification() calls backend /my endpoint which requires REVIEWER role JWT. Frontend only displays what backend returns. |
</threat_model>

<verification>
1. Frontend builds: `cd oaiss-chain-frontend && npm run build`
2. TypeScript check passes: `npx vue-tsc --noEmit`
3. Admin page accessible at /admin/certificates (ADMIN role only)
4. Enterprise UserProfile shows admission cert status
5. Auditor AuditList shows qualification status
6. i18n keys present in both zh-CN and en-US
</verification>

<success_criteria>
- CertificateManage.vue created with dual-tab layout (admission + qualification)
- 8 API functions added to admin.ts
- Route and menu config updated for /admin/certificates
- Enterprise UserProfile displays admission certificate status
- Auditor AuditList displays qualification status
- i18n translations complete for both locales
- Frontend builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-admission-qualification/10-03-SUMMARY.md`
</output>
