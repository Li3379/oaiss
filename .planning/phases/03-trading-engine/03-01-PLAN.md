---
phase: 03-trading-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/coin-balance-test.sh
autonomous: true
requirements:
  - COIN-01
  - COIN-02
  - COIN-03
  - COIN-04
  - COIN-05

must_haves:
  truths:
    - "Enterprise user can view carbon coin balance (balance field present, numeric)"
    - "Enterprise user can view paginated transaction history"
    - "Enterprise user can transfer coins to another enterprise"
    - "Both sender and receiver balances update atomically after transfer"
    - "Transfer exceeding balance is rejected with error code 4005"
  artifacts:
    - path: "scripts/coin-balance-test.sh"
      provides: "Carbon coin balance, history, transfer, and insufficient-balance tests"
      min_lines: 120
  key_links:
    - from: "scripts/coin-balance-test.sh"
      to: "/api/v1/carbon-coin/account"
      via: "curl GET"
      pattern: "carbon-coin/account"
    - from: "scripts/coin-balance-test.sh"
      to: "/api/v1/carbon-coin/transfer"
      via: "curl POST"
      pattern: "carbon-coin/transfer"
---

<objective>
Create a test script covering carbon coin account balance viewing, transaction history, peer-to-peer transfer, atomic balance verification, and insufficient-balance rejection.

Purpose: Verify the CarbonCoinController + CarbonCoinService subsystem works correctly for all 5 COIN requirements.
Output: `scripts/coin-balance-test.sh` -- executable bash script that tests COIN-01 through COIN-05.
</objective>

<execution_context>
@C:/Users/LiShuai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LiShuai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-trading-engine/03-RESEARCH.md
@.planning/phases/03-trading-engine/03-PATTERNS.md

<interfaces>
<!-- Key API contracts the executor needs from backend controllers -->

From CarbonCoinController.java (backend):
- GET /api/v1/carbon-coin/account -- returns {code, data:{id, userId, balance, totalRecharged, totalSpent, status}}
- GET /api/v1/carbon-coin/account?userId=X -- admin-only, returns same structure for specified user
- GET /api/v1/carbon-coin/transactions?page=1&size=10 -- returns {code, data:{content:[...], totalElements, totalPages, number, size}}
- POST /api/v1/carbon-coin/transfer -- body: {counterpartId:long, amount:BigDecimal, remark:String}
  - Success: {code:200, data:{id, userId, balance (updated), ...}}
  - Self-transfer: {code:4001, message:"cannot transfer to yourself"}
  - Counterpart not found: {code:4003}
  - Account disabled: {code:4004}
  - Insufficient balance: {code:4005, message:"carbon coin balance insufficient"}

Seed data:
- enterprise001 (userId=2): coin balance=10000, carbonTradable=38000
- enterprise002 (userId=3): coin balance=10000, carbonTradable=55000
- enterprise003 (userId=7): coin balance=10000, carbonTradable=50000

Response envelope: { "code": 200, "message": "success", "data": { ... } }
Error envelope: { "code": 4xxx, "message": "error description", "data": null }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create coin-balance-test.sh covering COIN-01 through COIN-05</name>
  <files>scripts/coin-balance-test.sh</files>
  <read_first>
    - scripts/carbon-report-test.sh (skeleton pattern: set -euo pipefail, color helpers, extract_field, login function, TOTAL/PASSED/FAILED counters, summary block)
    - .planning/phases/03-trading-engine/03-RESEARCH.md (API endpoints, DTOs, error codes, seed data)
    - .planning/phases/03-trading-engine/03-PATTERNS.md (exact script skeleton code, assertion patterns, negative test patterns)
  </read_first>
  <action>
Create `scripts/coin-balance-test.sh` following the exact skeleton from `scripts/carbon-report-test.sh` (lines 1-60: shebang, set -euo pipefail, color helpers, extract_field, login function, health check, login calls).

The script must include these test cases in order:

**Setup section:**
1. Health check: `curl -sf "$API/swagger-ui.html" -o /dev/null`
2. Login as enterprise001 (TOKEN_E1), enterprise002 (TOKEN_E2), and admin (TOKEN_ADMIN)

**COIN-01: View carbon coin balance**
- GET /api/v1/carbon-coin/account with TOKEN_E1
- Assert code==200, data.balance is present and numeric
- Print balance value for traceability

**COIN-02: View transaction history (paginated)**
- GET /api/v1/carbon-coin/transactions?page=1&size=10 with TOKEN_E1
- Assert code==200, data structure has content array (may be empty since no transactions yet)
- Verify pagination fields present (totalElements, totalPages)

**COIN-03: Transfer coins from enterprise001 to enterprise002**
- Record E1 balance before: GET /api/v1/carbon-coin/account with TOKEN_E1, store as BALANCE_E1_BEFORE
- Record E2 balance before: GET /api/v1/carbon-coin/account?userId=3 with TOKEN_ADMIN, store as BALANCE_E2_BEFORE
- POST /api/v1/carbon-coin/transfer with TOKEN_E1, body: {"counterpartId": 3, "amount": 500, "remark": "Phase3 test transfer"}
- Assert code==200

**COIN-04: Verify atomic balance update after transfer**
- GET /api/v1/carbon-coin/account with TOKEN_E1, extract balance as BALANCE_E1_AFTER
- Assert BALANCE_E1_AFTER == BALANCE_E1_BEFORE - 500 (use bash arithmetic)
- GET /api/v1/carbon-coin/account?userId=3 with TOKEN_ADMIN, extract balance as BALANCE_E2_AFTER
- Assert BALANCE_E2_AFTER == BALANCE_E2_BEFORE + 500
- If either assertion fails, print expected vs actual values

**COIN-05: Insufficient balance rejected**
- POST /api/v1/carbon-coin/transfer with TOKEN_E1, body: {"counterpartId": 3, "amount": 999999, "remark": "should fail"}
- Assert code != 200 (should be 4005)
- Print the error code for traceability

**Summary block:**
Print total/passed/failed counts with the standard format from carbon-report-test.sh (colored output).
Exit 1 if any failures.

**Key implementation details:**
- All balance comparisons must use bash integer arithmetic: `$(( BALANCE_BEFORE - 500 ))`. Extract balance as integer (strip decimals if present).
- The extract_field helper uses grep + sed, same as carbon-report-test.sh lines 21-24.
- Use the negative test pattern from PATTERNS.md for COIN-05: check that code != 200.
- The script must be executable (shebang line at top).
  </action>
  <verify>
    <automated>bash scripts/coin-balance-test.sh 2>&1 | grep -E '\[OK\]|\[FAIL\]|Total:|Passed:|Failed:' | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File scripts/coin-balance-test.sh exists and is executable
    - Script contains string "COIN-01" (test case for balance viewing)
    - Script contains string "COIN-02" (test case for transaction history)
    - Script contains string "COIN-03" (test case for transfer)
    - Script contains string "COIN-04" (test case for atomic balance update)
    - Script contains string "COIN-05" (test case for insufficient balance)
    - Script contains "set -euo pipefail"
    - Script contains "carbon-coin/account"
    - Script contains "carbon-coin/transfer"
    - Script contains "carbon-coin/transactions"
    - Script contains TOTAL/PASSED/FAILED counter pattern
    - Running the script outputs "Total:", "Passed:", "Failed:" summary line
  </acceptance_criteria>
  <done>All 5 COIN requirements tested: balance viewing works, transaction history paginated, transfer succeeds, both balances update atomically by +/-500, insufficient balance returns error code 4005. Script exits 0 on all pass, exits 1 on any failure.</done>
</task>

</tasks>

<verification>
1. Run `bash scripts/coin-balance-test.sh` -- expect 0 failures, all 5 tests pass
2. Verify balance arithmetic: after transfer of 500, sender balance decreased by 500, receiver increased by 500
3. Verify insufficient balance test: requesting transfer of 999999 returns non-200 code
</verification>

<success_criteria>
- `bash scripts/coin-balance-test.sh` completes with 0 failures
- All 5 test cases (COIN-01 through COIN-05) report [OK]
- Balance verification shows correct +/-500 update on both accounts
- Insufficient balance test shows correct error code (4005)
</success_criteria>

<output>
After completion, create `.planning/phases/03-trading-engine/03-01-SUMMARY.md`
</output>
