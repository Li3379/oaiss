---
wave: 1
depends_on: [17-01]
requirements_addressed: [I18N-01]
files_modified:
  - oaiss-chain-frontend/src/views/admin/VerifyList.vue
  - oaiss-chain-frontend/src/views/third-party/Monitor.vue
  - oaiss-chain-frontend/src/api/auth.ts
  - oaiss-chain-frontend/src/api/emission.ts
  - oaiss-chain-frontend/src/i18n/locales/zh-CN.ts
  - oaiss-chain-frontend/src/i18n/locales/en-US.ts
autonomous: true
---

# Plan 17-02: i18n Cleanup — Extract Hardcoded Chinese

## Objective
Extract 4 hardcoded Chinese strings from 4 files into vue-i18n keys. Add translations to zh-CN.ts and en-US.ts.

## Context
CONTEXT.md D-03 identifies 4 files with hardcoded Chinese visible to users. CONTEXT.md D-04 establishes the pattern: import i18n singleton, use `i18n.global.t('key')` in API files; use `$t()` / `t()` in Vue components.

## Tasks

### Task 1: Extract hardcoded Chinese from VerifyList.vue
<read_first>
- oaiss-chain-frontend/src/views/admin/VerifyList.vue
- oaiss-chain-frontend/src/i18n/locales/zh-CN.ts
- oaiss-chain-frontend/src/i18n/locales/en-US.ts
</read_first>

<action>
1. In VerifyList.vue line ~123, find the status map: `{ ..., 5: '已上链' }`
2. Replace `'已上链'` with `t('verifyList.statusOnChain')`
3. Add to zh-CN.ts under `verifyList` section: `statusOnChain: '已上链'`
4. Add to en-US.ts under `verifyList` section: `statusOnChain: 'On Chain'`
</action>

<acceptance_criteria>
- VerifyList.vue no longer contains literal string '已上链'
- VerifyList.vue uses `t('verifyList.statusOnChain')` for status code 5
- zh-CN.ts verifyList section contains `statusOnChain: '已上链'`
- en-US.ts verifyList section contains `statusOnChain: 'On Chain'`
</acceptance_criteria>

### Task 2: Extract hardcoded Chinese from Monitor.vue
<read_first>
- oaiss-chain-frontend/src/views/third-party/Monitor.vue
- oaiss-chain-frontend/src/i18n/locales/zh-CN.ts
- oaiss-chain-frontend/src/i18n/locales/en-US.ts
</read_first>

<action>
1. In Monitor.vue line ~138, find: `{{ t('monitor.statApprovalRate') || '审核通过率' }}`
2. Remove the fallback: change to `{{ t('monitor.statApprovalRate') }}`
3. Verify zh-CN.ts already has `statApprovalRate: '审核通过率'` — it does (line 702)
4. Verify en-US.ts has the corresponding entry — add if missing: `statApprovalRate: 'Approval Rate'`
</action>

<acceptance_criteria>
- Monitor.vue no longer contains `|| '审核通过率'` fallback
- zh-CN.ts monitor section contains `statApprovalRate: '审核通过率'`
- en-US.ts monitor section contains `statApprovalRate: 'Approval Rate'`
</acceptance_criteria>

### Task 3: Extract hardcoded Chinese from auth.ts
<read_first>
- oaiss-chain-frontend/src/api/auth.ts
- oaiss-chain-frontend/src/i18n/index.ts
- oaiss-chain-frontend/src/i18n/locales/zh-CN.ts
- oaiss-chain-frontend/src/i18n/locales/en-US.ts
</read_first>

<action>
1. Add `import i18n from '@/i18n'` at top of auth.ts (if not already imported)
2. Replace `'用户名不能为空'` with `i18n.global.t('auth.usernameRequired')`
3. Replace `'密码不能为空'` with `i18n.global.t('auth.passwordRequired')`
4. Add to zh-CN.ts under new `auth` section: `usernameRequired: '用户名不能为空'`, `passwordRequired: '密码不能为空'`
5. Add to en-US.ts under `auth` section: `usernameRequired: 'Username is required'`, `passwordRequired: 'Password is required'`
</action>

<acceptance_criteria>
- auth.ts imports i18n from '@/i18n'
- auth.ts no longer contains literal string '用户名不能为空'
- auth.ts no longer contains literal string '密码不能为空'
- zh-CN.ts has auth.usernameRequired and auth.passwordRequired
- en-US.ts has auth.usernameRequired and auth.passwordRequired
</acceptance_criteria>

### Task 4: Extract hardcoded Chinese from emission.ts
<read_first>
- oaiss-chain-frontend/src/api/emission.ts
- oaiss-chain-frontend/src/i18n/locales/zh-CN.ts
- oaiss-chain-frontend/src/i18n/locales/en-US.ts
</read_first>

<action>
1. Add `import i18n from '@/i18n'` at top of emission.ts (if not already imported)
2. Replace `'企业ID不能为空'` with `i18n.global.t('emissionData.enterpriseIdRequired')`
3. Add to zh-CN.ts under `emissionData` section: `enterpriseIdRequired: '企业ID不能为空'`
4. Add to en-US.ts under `emissionData` section: `enterpriseIdRequired: 'Enterprise ID is required'`
</action>

<acceptance_criteria>
- emission.ts imports i18n from '@/i18n'
- emission.ts no longer contains literal string '企业ID不能为空'
- zh-CN.ts emissionData section has enterpriseIdRequired
- en-US.ts emissionData section has enterpriseIdRequired
</acceptance_criteria>

## Verification Criteria
- All 5 success criteria from ROADMAP Phase 17 are met:
  4. 4 files have hardcoded Chinese extracted to i18n keys
  5. zh-CN.ts and en-US.ts contain corresponding translations
- No visual regression — i18n resolves same Chinese text at runtime
