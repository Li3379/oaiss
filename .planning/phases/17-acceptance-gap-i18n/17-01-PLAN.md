---
wave: 1
depends_on: []
requirements_addressed: [GAP-01, GAP-02]
files_modified:
  - oaiss-chain-frontend/tests/e2e/v1.1/blockchain-formula-flow.spec.ts
  - oaiss-chain-frontend/tests/e2e/v1.1/ai-prediction-flow.spec.ts
  - oaiss-chain-frontend/tests/e2e/utils/coverage-report.ts
autonomous: true
---

# Plan 17-01: Verify GAP Tests Work Post Phase 16

## Objective
Confirm that GAP-01 (REQ-06 carbon formula) and GAP-02 (REQ-03 emission predict) E2E tests are already covered by existing test code, and that CORE_ENDPOINTS entries exist. No new test code — verification only.

## Context
Phase 16 fixed the auth fixture timeout (Content-Type header in loginViaApi). The GAP tests were written in prior phases but couldn't run due to the auth fixture issue. Now they should work. CONTEXT.md D-01 confirms: tests and CORE_ENDPOINTS already exist, no new code needed.

## Tasks

### Task 1: Verify carbon formula E2E test coverage (GAP-01)
<read_first>
- oaiss-chain-frontend/tests/e2e/v1.1/blockchain-formula-flow.spec.ts
</read_first>

<action>
Read blockchain-formula-flow.spec.ts and confirm it contains:
1. Test describe block for "Power Generation Formula (REQ-06)" with POST to `/carbon/calculate/power-generation`
2. Test describe block for "Power Grid Formula (REQ-06)" with POST to `/carbon/calculate/power-grid`
3. At least 2 test cases per formula (happy path + validation)

Document findings in 17-01-SUMMARY.md under "GAP-01 Verification" section.
</action>

<acceptance_criteria>
- blockchain-formula-flow.spec.ts contains "Power Generation Formula (REQ-06)" test describe
- blockchain-formula-flow.spec.ts contains "Power Grid Formula (REQ-06)" test describe
- At least 4 total test cases across both formula sections
- 17-01-SUMMARY.md created with GAP-01 verification section
</acceptance_criteria>

### Task 2: Verify emission prediction E2E test coverage (GAP-02)
<read_first>
- oaiss-chain-frontend/tests/e2e/v1.1/ai-prediction-flow.spec.ts
</read_first>

<action>
Read ai-prediction-flow.spec.ts and confirm it contains:
1. Test describe block for emission prediction with POST to `/emission/predict`
2. At least 3 test cases: happy path, data point validation, missing required field

Document findings in 17-01-SUMMARY.md under "GAP-02 Verification" section.
</action>

<acceptance_criteria>
- ai-prediction-flow.spec.ts contains test case POSTing to `/emission/predict`
- At least 3 test cases for emission prediction
- 17-01-SUMMARY.md updated with GAP-02 verification section
</acceptance_criteria>

### Task 3: Verify CORE_ENDPOINTS coverage
<read_first>
- oaiss-chain-frontend/tests/e2e/utils/coverage-report.ts
</read_first>

<action>
Read coverage-report.ts and confirm CORE_ENDPOINTS contains:
1. `/carbon/calculate/power-generation` POST with category 'v1.1'
2. `/carbon/calculate/power-grid` POST with category 'v1.1'
3. `/emission/predict` POST with category 'v1.1'
4. All three mapped to correct test files (blockchain-formula-flow, ai-prediction-flow)

Document findings in 17-01-SUMMARY.md under "CORE_ENDPOINTS Verification" section.
</action>

<acceptance_criteria>
- CORE_ENDPOINTS includes entry for `/carbon/calculate/power-generation` (POST, v1.1)
- CORE_ENDPOINTS includes entry for `/carbon/calculate/power-grid` (POST, v1.1)
- CORE_ENDPOINTS includes entry for `/emission/predict` (POST, v1.1)
- 17-01-SUMMARY.md updated with CORE_ENDPOINTS verification section
</acceptance_criteria>

## Verification Criteria
- All 5 success criteria from ROADMAP Phase 17 are met:
  1. blockchain-formula-flow.spec.ts covers power-generation and power-grid
  2. ai-prediction-flow.spec.ts contains /emission/predict test cases
  3. CORE_ENDPOINTS contains all three endpoints
- 17-01-SUMMARY.md documents all verification evidence
