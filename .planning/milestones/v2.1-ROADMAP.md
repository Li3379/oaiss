# Milestone v2.1: 测试基础设施修复与收尾

**Status:** SHIPPED 2026-05-22
**Phases:** 16-18
**Total Plans:** 5

## Overview

修复 E2E 测试基础设施（auth fixture timeout 导致 68 flow 测试失败），补齐 v1.1.0 验收缺口（REQ-06 碳核算公式 E2E、REQ-03 /emission/predict），清理 i18n 残留，完成 Fabric CA 可选集成。

## Phases

### Phase 16: E2E 测试基础设施修复

**Goal**: 修复 auth fixture timeout，接入 d9/d10 孤悬测试，接入 isFabricAvailable() 钩子
**Depends on**: Phase 15 (v2.0 shipped)
**Plans**: 2 plans

Plans:

- [x] 16-01 — Auth Fixture Fix: 修复 loginViaApi token 提取，恢复 flow/v1.1 测试运行 (E2E-01)
- [x] 16-02 — Test Discovery Fix: 迁移 d9/d10 到 v1.1 目录，创建缺失 page objects，接入 isFabricAvailable (E2E-02, E2E-03)

**Details:**
- Fixed `loginViaApi()` Content-Type header issue in `fixtures/auth.ts`
- Migrated d9/d10 specs to `tests/e2e/v1.1/` directory
- Created `BlockchainExplorerPage.ts` page object
- Wired `isFabricAvailable()` in `blockchain-formula-flow.spec.ts`

### Phase 17: 验收缺口补齐与 i18n 清理

**Goal**: 补齐 REQ-06/REQ-03 E2E 测试，提取硬编码中文到 i18n
**Depends on**: Phase 16 (auth fixture 必须先修复)
**Plans**: 2 plans

Plans:

- [x] 17-01 — E2E Gap Tests: 碳核算公式测试 + 排放预测测试 + CORE_ENDPOINTS 更新 (GAP-01, GAP-02)
- [x] 17-02 — i18n Cleanup: 提取 4 文件 7 处硬编码中文到 vue-i18n keys (I18N-01)

**Details:**
- Created `blockchain-formula-flow.spec.ts` with 6 test cases for power-generation + power-grid
- Created `ai-prediction-flow.spec.ts` with 5 test cases for `/emission/predict`
- Updated `CORE_ENDPOINTS` in `coverage-report.ts`
- Extracted hardcoded Chinese from VerifyList.vue, Monitor.vue, auth.ts, emission.ts

### Phase 18: Fabric CA 可选集成

**Goal**: 实现 FabricCAService registerEnrollment，集成到现有 Fabric 区块链流程
**Depends on**: Phase 16 (E2E 基础设施就绪)
**Plans**: 1 plan

Plans:

- [x] 18-01 — Fabric CA Integration: FabricCAService registerEnrollment + @Profile("fabric") 条件装配 (FABRIC-01)

**Details:**
- Implemented `FabricCAService` with `registerEnrollment()` method
- Added conditional identity resolution with static crypto fallback
- `@Profile("fabric")` conditional assembly for CA-enabled environments
- 4/4 FabricCAServiceTest passed, 3/3 FabricGatewayConfigTest passed

---

## Milestone Summary

**Key Decisions:**

- D-01: EnrollmentResult made public (not package-private) for cross-package access from FabricGatewayConfig

**Issues Resolved:**

- Auth fixture timeout blocking 68+ flow tests
- d9/d10 specs orphaned (not in any testDir)
- isFabricAvailable() exported but unused
- REQ-06 carbon formulas zero E2E coverage
- REQ-03 /emission/predict zero E2E coverage
- 4 files with hardcoded Chinese strings
- Fabric CA no application-layer integration

**Issues Deferred:**

- CarbonFormulaCalculatorPage.ts page object (d10 uses inline selectors)
- E2E smoke route failure (needs frontend routing investigation)
- Prophet Windows fix (CmdStan environment issue)
- Auction order pagination (PERF-04 design reserve)

**Technical Debt Incurred:**

- CA TLS handshake verification requires running Fabric CA Docker stack (manual-only)
- Enrollment persistence across restarts requires JVM restart + CA state (manual-only)
- Flow test pass rate requires backend + frontend running (HUMAN_NEEDED)

---

_For current project status, see .planning/ROADMAP.md_
